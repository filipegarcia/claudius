# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/user/application_security/sast/#available-cicd-variables
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure/
# Dependency Scanning customization: https://docs.gitlab.com/user/application_security/dependency_scanning/dependency_scanning_sbom/#customizing-analyzer-behavior
# Container Scanning customization: https://docs.gitlab.com/user/application_security/container_scanning/#customizing-analyzer-behavior
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ci/variables/#cicd-variable-precedence
stages:
- test
- secret-detection
- deploy

include:
- template: Security/SAST.gitlab-ci.yml
- template: Security/Secret-Detection.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: 'true'

sast:
  stage: test

secret_detection:
  stage: secret-detection

.node_cache: &node_cache
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
      - .npm/

lint:
  stage: test
  image: node:22-bookworm
  <<: *node_cache
  script:
    - make install
    - make lint

e2e:
  stage: test
  image: mcr.microsoft.com/playwright:v1.59.1-jammy
  <<: *node_cache
  before_script:
    # The Playwright Jammy image doesn't ship with `make`. Install it so
    # `make install` / `make test` work the same as locally and a future
    # Makefile change is automatically picked up in CI.
    - apt-get update -qq && apt-get install -y --no-install-recommends make
  script:
    - make install
    - make test
  artifacts:
    when: always
    paths:
      - playwright-report/
      - test-results/
    expire_in: 1 week

# Marketing site is published from site/. Screenshots in site/screenshots/
# are committed (generated locally via `make screenshots`), so this job
# just bundles the directory.
pages:
  stage: deploy
  image: alpine:3
  script:
    - 'true'
  pages:
    publish: site
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
