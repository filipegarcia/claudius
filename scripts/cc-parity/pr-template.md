<!--
  PR body template for automated cc-parity branches.
  `orchestrate.ts` reads this file, substitutes the {{...}} placeholders,
  inlines the matching sections from `.claudius/cc-parity/run-notes/<version>.md`,
  and passes the result to `gh pr create --body`.

  Placeholders:
    {{NEW_VERSION}}            e.g. 1.0.40
    {{PREVIOUS_VERSION}}       e.g. 1.0.39
    {{CHANGELOG_URL}}          compare URL on anthropics/claude-code
    {{CHANGELOG_BODY}}         verbatim claude-code changelog excerpt PREV..NEW
    {{NOTES_SUMMARY}}          "## Summary" section from run-notes
    {{NOTES_CLASSIFICATION}}   "## Changelog classification" from run-notes
    {{NOTES_IMPLEMENTED}}      "## Implemented (bucket B)" from run-notes
    {{NOTES_UI}}               "## New UI surfaces" from run-notes
    {{NOTES_TESTS}}            "## Tests" from run-notes
    {{NOTES_RISKS}}            "## Risks / follow-ups" from run-notes
    {{SCREENSHOTS_BLOCK}}      auto-built list of ![alt](raw.githubusercontent.com URLs)
                               for every PNG under docs/cc-parity/{{NEW_VERSION}}/
    {{BUDGET_STATUS}}          either "" (clean run) or a warning block when the
                               PR was opened in draft because the budget was hit
-->

# claude-code parity `{{PREVIOUS_VERSION}}` → `{{NEW_VERSION}}`

> ⚠️ **Auto-generated by the Claude Code parity pipeline.**
> Treat each implemented item below as a **design proposal**, not
> a finished change. Green CI proves nothing broke, NOT that any of
> these match Claudius's intended UX. Read the **Changelog
> classification** section to confirm the bot's A/B/C bucketing
> before approving — a misclassified entry is the most common way
> this pipeline ships the wrong thing.

{{BUDGET_STATUS}}

{{NOTES_SUMMARY}}

## Upstream changelog

[Compare on GitHub →]({{CHANGELOG_URL}})

<details>
<summary>Raw claude-code changelog between <code>v{{PREVIOUS_VERSION}}</code> and <code>v{{NEW_VERSION}}</code></summary>

{{CHANGELOG_BODY}}

</details>

## Changelog classification (A / B / C)

This is where the bot bucketed each substantive entry. Items tagged
`[A]` are already covered by the SDK updater and are intentionally
NOT implemented here. Items tagged `[B]` are the work in this PR.
Items tagged `[C]` are terminal-only and intentionally skipped.

If you disagree with a bucketing, flag it on the PR — that's the
single highest-leverage review action for this pipeline.

{{NOTES_CLASSIFICATION}}

## Implemented (bucket B)

{{NOTES_IMPLEMENTED}}

## New UI surfaces

{{NOTES_UI}}

{{SCREENSHOTS_BLOCK}}

## Tests

{{NOTES_TESTS}}

## Risks / follow-ups

{{NOTES_RISKS}}

---

<sub>
Opened automatically by `scripts/cc-parity/orchestrate.ts`.
The body is generated from `scripts/cc-parity/pr-template.md` and the
run-notes file at `.claudius/cc-parity/run-notes/{{NEW_VERSION}}.md`.
</sub>
