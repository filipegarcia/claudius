# SDK update 0.3.181 → 0.3.183

<!--
  This file is the PRIMARY DELIVERABLE for the SDK-update bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

SDK 0.3.183 (and 0.3.182) updated Claudius to parity with Claude Code v2.1.182 and v2.1.183. Both releases carry an opaque "Updated to parity" changelog; a precise type diff was obtained via `npm pack @anthropic-ai/claude-agent-sdk@0.3.181` + `diff` against the installed 0.3.183 `sdk.d.ts`. The diff produced four hunks: (1) `SDKControlSetMcpPermissionModeOverrideRequest` added to the SDK-internal `SDKControlRequestInner` union; (2) `attribution.sessionUrl?: boolean` added to `Settings`; (3) `disableClaudeAiConnectors?: boolean` added to `Settings`; (4) `waitForExit?()` added to the SDK-internal `Transport` interface. All are additive, optional, and non-breaking — none require Claudius changes (Settings passthrough via `[key: string]: unknown`, Transport not implemented by Claudius). No model renames or capability-tier changes. The build (`bun run build`), linter (`bun run lint`), unit suite (`bun run test`: 1337 passed), and e2e suite (`bun run test:e2e`: 125 passed, 7 skipped expectedly) are all green on the 0.3.183 tree. No code changes were needed.

## SDK changelog highlights

**0.3.183 — Updated to parity with Claude Code v2.1.183:**

Verified via `npm pack @anthropic-ai/claude-agent-sdk@0.3.181` + `diff package/sdk.d.ts node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts`. Four concrete hunks:

- `[skipped — SDK-internal control wire]` `SDKControlSetMcpPermissionModeOverrideRequest` added to `SDKControlRequestInner` union (inserted between `SDKControlMcpToggleRequest` and `SDKControlChannelEnableRequest`). SDK-internal dispatching plumbing. Claudius never switches on `SDKControlRequestInner` directly — it uses the typed `Query` wrapper methods (`interrupt()`, `setModel()`, etc.). No action required.

- `[skipped — passthrough settings field]` `attribution.sessionUrl?: boolean` added to the SDK `Settings` interface. Controls whether the claude.ai session link is appended to commits/PRs from web/Remote Control sessions (default: true). The `ClaudeSettings` type in `lib/server/settings.ts` has a `[key: string]: unknown` catch-all at line 94 — the field passes through unmodified. The `app/settings/page.tsx` catalog comment at line 848 explicitly says "complex nested objects (worktree, attribution, hooks…) are omitted" — they fall through to the generic "Other" editor. No action required.

- `[skipped — passthrough settings field]` `disableClaudeAiConnectors?: boolean` added to the SDK `Settings` interface. Gates auto-fetching of claude.ai MCP cloud connectors. Passes through `ClaudeSettings`'s `[key: string]: unknown` catch-all. Not relevant to Claudius's MCP server management (Claudius configures explicit MCP servers, not auto-fetched claude.ai connectors). No action required.

- `[skipped — SDK-internal transport interface]` `waitForExit?(): Promise<void>` added to the `Transport` interface. JSDoc: "Only meaningful for subprocess-backed transports (ProcessTransport); WebSocket / SSE / in-process transports leave this undefined. Query.performCleanup() awaits it…". Claudius doesn't implement `Transport` — it constructs sessions via `query()` from the SDK. No action required.

**0.3.182 — Updated to parity with Claude Code v2.1.182:**

- `[skipped — runtime parity, no enumerated public API delta]` The diff between 0.3.181 and 0.3.183 covers both 0.3.182 and 0.3.183. All four changed symbols are additive optional extensions (a new union member and three new optional fields/methods); none are breaking. `bun run build` confirms no type breakage across the full 0.3.181→0.3.183 upgrade.

**Model-identity audit (required per "second failure mode" rules):**

- `app/api/models/route.ts` (`STATIC_FALLBACK`) — Checked. Uses aliases (`default`, `sonnet`, `haiku`, `opus`, `fable`) rather than pinned model IDs. No version-specific string requires updating. The fable row already carries the correct description, `supportsEffort: true`, `supportedEffortLevels: ["low","medium","high","xhigh","max"]`, and `supportsAdaptiveThinking: true`. No change required.
- `app/api/models/probe/route.ts` — Checked. Probes `claude-fable-5`, `claude-opus-4-8`, `claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-haiku-4-5`. The type diff confirms no model-related type changes in the SDK surface between 0.3.181 and 0.3.183. No change required.
- `app/api/sessions/[id]/model/route.ts` — Checked. Uses the SDK `supportedModels()` live call for the session-scoped picker; no hardcoded model ID list. No change required.
- `lib/client/types.ts` — Checked. No `ModelInfo` type definition here; model data flows in from the API routes. No change required.
- `lib/server/session.ts` — Checked. The fable-advisor-incompatibility clearing (`if (this.advisorModel && this.model?.includes("fable"))`) is still correct. The `enable1mContext` gate (`context-1m-2025-08-07` beta header) remains appropriate — this is Sonnet 4/4.5 only and the v2.1.182/183 changelog contains no 1M-context default change. No change required.
- `lib/server/litellm-pricing.ts` + `lib/server/litellm-prices.json` — Checked. No model pricing tier change was announced. No change required.
- `components/chat/ModelPicker.tsx` — Checked. The local `ModelInfo` mirror already includes `supportsFastMode` and `supportsAutoMode` (both present in the SDK type since before 0.3.181). Picker copy is accurate. No change required.

## Code changes

- No code changes required. Both 0.3.182 and 0.3.183 are runtime-parity releases with no public SDK API additions. The build passes cleanly, all imports resolve, and all tests are green. No Claudius file needed to be touched to support the new SDK version.

## New UI surfaces

- No new UI surfaces this release. Neither 0.3.182 nor 0.3.183 introduced any new SDK-level feature that Claudius exposes in the browser (no new SSE event type, no new tool, no new permission mode, no new dialog kind, no new `Query` method relevant to Claudius). All model-identity surfaces were audited and found current.

## Tests

- vitest: 0 new specs, 0 updated — no behaviour changed.
- playwright: 0 new specs, 0 updated — no UI surface changed.
- All 1337 unit tests pass; 125 e2e tests pass (7 skipped: voice-dictation live test, always skipped in CI).

## Risks / follow-ups

- The precise type diff confirms no model renames or capability-tier changes in the TypeScript surface. However, if Anthropic silently retired a model at the binary level (inside the `.zst` CLI bundle, not surfaced in the TypeScript types), Claudius would be unaffected on the type side but could show stale copy on the probe page. A reviewer with `claude-fable-5` entitlement can confirm the probe route still returns `ok: true` for all candidates after deploying this SDK bump. No actionable item — the models list and descriptions are as accurate as the TypeScript surface allows.
