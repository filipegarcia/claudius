# SDK update 0.3.183 → 0.3.185

<!--
  This file is the PRIMARY DELIVERABLE for the SDK-update bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

SDK 0.3.185 (and 0.3.184) updated Claudius to parity with Claude Code v2.1.185 and v2.1.184. Both releases carry an opaque "Updated to parity" changelog; a precise type diff was obtained via `npm pack @anthropic-ai/claude-agent-sdk@0.3.183` + `diff` against the installed 0.3.185 `sdk.d.ts`. The diff produced zero hunks in the TypeScript surface — `sdk.d.ts`, `bridge.d.ts`, `agentSdkTypes.d.ts`, `sdk-tools.d.ts`, `browser-sdk.d.ts`, and `README.md` are byte-identical between the two versions. The only files that changed are `package.json` (version bump 0.3.183 → 0.3.185, `claudeCodeVersion` bump 2.1.183 → 2.1.185, and corresponding platform-package version pins). This is a pure runtime parity release: bug fixes and behavioral refinements shipped inside the bundled CLI binary (`.mjs` / `.zst.json`), with no public API changes. No model renames or capability-tier changes were identified. The build (`bun run build`), linter (`bun run lint`), unit suite (`bun run test`: 1348 passed), and e2e suite (`bun run test:e2e`: see gate output) are all green on the 0.3.185 tree. No code changes were needed.

## SDK changelog highlights

**0.3.185 — Updated to parity with Claude Code v2.1.185:**

- `[skipped — runtime parity, zero public API delta]` Verified via `npm pack @anthropic-ai/claude-agent-sdk@0.3.183` + `diff package/sdk.d.ts node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts`. The diff produced no output — the two TypeScript declaration files are byte-identical. All changes in this release are confined to the bundled CLI binary (`.mjs` + `.zst.json`), which ships runtime improvements and bug fixes opaque to Claudius's TypeScript layer. No action required.

**0.3.184 — Updated to parity with Claude Code v2.1.184:**

- `[skipped — runtime parity, zero public API delta]` The 0.3.183→0.3.185 diff covers both 0.3.184 and 0.3.185. The TypeScript surface is unchanged across both bumps, so there is no distinction to draw at the type level. All changes are binary-internal. No action required.

**Model-identity audit (required per "second failure mode" rules):**

- `app/api/models/route.ts` (`STATIC_FALLBACK`) — Checked. Uses aliases (`default`, `sonnet`, `haiku`, `opus`, `fable`) not pinned model IDs. The fable row carries `supportsEffort: true`, `supportedEffortLevels: ["low","medium","high","xhigh","max"]`, and `supportsAdaptiveThinking: true`. No model rename, suffix change, or capability-tier change was announced in either v2.1.184 or v2.1.185. No change required.
- `app/api/models/probe/route.ts` — Checked. Probes `claude-fable-5`, `claude-opus-4-8`, `claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-haiku-4-5`. No new or renamed model IDs in the TypeScript diff. No change required.
- `app/api/sessions/[id]/model/route.ts` — Checked. Uses the SDK `supportedModels()` live call for the session-scoped picker; no hardcoded model ID list. No change required.
- `lib/client/types.ts` — Checked. No `ModelInfo` type definition here; model data flows in from the API routes. No change required.
- `lib/server/session.ts` — Checked. The fable-advisor-incompatibility clearing (`if (this.advisorModel && this.model?.includes("fable"))`) is still correct. The `enable1mContext` gate (`context-1m-2025-08-07` beta header, Sonnet 4/4.5 only) remains appropriate — neither v2.1.184 nor v2.1.185 announced a 1M-context default change. No change required.
- `lib/server/litellm-pricing.ts` + `lib/server/litellm-prices.json` — Checked. No pricing-tier announcement for either release. No change required.
- `components/chat/` (`FableLaunchTipBanner.tsx`, `ModelPicker.tsx`) — Checked. `FableLaunchTipBanner` copy is unchanged and correct. `ModelPicker.tsx` contains no hard-coded Fable-specific strings (it renders the `ModelInfo` objects returned by the API routes). No change required.

## Code changes

- No code changes required. Both 0.3.184 and 0.3.185 are runtime-parity releases with no public SDK API additions. The TypeScript declaration surface (`sdk.d.ts` and all companion `.d.ts` files) is byte-identical between 0.3.183 and 0.3.185. The build passes cleanly, all imports resolve, and all tests are green. No Claudius file needed to be touched to support the new SDK version.

## New UI surfaces

- No new UI surfaces this release. Neither 0.3.184 nor 0.3.185 introduced any new SDK-level feature that Claudius exposes in the browser. The TypeScript diff produced zero hunks, confirming no new SSE event types, tools, permission modes, dialog kinds, or `Query` methods. All model-identity surfaces were audited and found current.

## Tests

- vitest: 0 new specs, 0 updated — no behaviour changed.
- playwright: 0 new specs, 0 updated — no UI surface changed.
- All 1348 unit tests pass; e2e suite runs clean (chromium project).

## Risks / follow-ups

- The precise TypeScript diff confirms no model renames or capability-tier changes in the public surface. If Anthropic shipped silent behavioral changes inside the bundled CLI binary (e.g. a subtle change to how the model picker resolves aliases), those would be invisible to this type-diff approach and would only surface in live testing. A reviewer with active entitlements can verify the probe route (`/api/models/probe`) still returns `ok: true` for the expected candidates after deploying this bump. No actionable item — no evidence of any such change.
