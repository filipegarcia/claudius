# SDK update 0.3.165 → 0.3.166

<!--
  This file is the PRIMARY DELIVERABLE for the SDK-update bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

SDK 0.3.166 ships a single internal bug fix: MCP resource tools were not being
injected for servers added at runtime via the `mcp_set_servers` control request.
The fix is entirely inside the SDK — no public API surface changed. Claudius already
calls `setMcpServers` in `lib/server/session.ts` (backed by the `POST /api/sessions/[id]/mcp-dynamic`
route) and benefits from the fix automatically with the version bump. No Claudius
code changes were required. Headline risk: none — this is a narrowly-scoped regression
fix for a code path Claudius already exercises.

## SDK changelog highlights

- Fixed MCP resource tools not being injected for servers added at runtime via
  the `mcp_set_servers` control request.
  [skipped — internal SDK bug fix; no public API surface changed. Claudius calls
  `Query.setMcpServers` / `session.setMcpServers` and benefits from the fix
  automatically via the version bump. No code changes required on our side.]

## Code changes

- No code changes required. The changelog entry is an internal SDK bug fix with no
  public API surface changes. Claudius's `setMcpServers` wrapper in
  `lib/server/session.ts` and the `POST /api/sessions/[id]/mcp-dynamic` route that
  calls it are both already correct; the SDK now correctly injects MCP resource tools
  for runtime-added servers alongside the existing tool injection it already handled.

## New UI surfaces

- No new UI surfaces this release. The SDK change is an internal bug fix only; no
  new user-visible behaviour was added by this version bump.

## Tests

- No new tests required. The fix is internal to the SDK; Claudius has no call-site
  changes to exercise. The existing e2e suite (including the MCP-related flows)
  continues to cover the `mcp-dynamic` route and `setMcpServers` path.

## Risks / follow-ups

- None identified. The bug fix makes runtime-added MCP servers expose their resource
  tools correctly, which is strictly an improvement; any existing usage that relied on
  resource tools being absent from runtime-added servers would already have been
  broken before this fix.
