# SDK update 0.3.191 → 0.3.193

<!--
  This file is the PRIMARY DELIVERABLE for the SDK-update bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

SDK 0.3.192 brought parity with Claude Code v2.1.192; SDK 0.3.193 added `promptSuggestions` to the Browser SDK (`@anthropic-ai/claude-agent-sdk/browser`) `BrowserQueryOptions` and fixed brief console window flashes on Windows when spawning CLI subprocesses. Neither change requires code edits in Claudius: the Browser SDK subpath is not used — Claudius runs the main SDK server-side — and `promptSuggestions` was already wired into the main `query()` Options call (commit a5b3793, `feat(agents): enable SDK prompt suggestions`) long before this release formalised it in the Browser SDK type. All four gates (lint, vitest, build, Playwright e2e) are green with no code changes.

## SDK changelog highlights

- **0.3.193** — Added `promptSuggestions` option to Browser SDK `query()` to opt the remote CLI into emitting follow-up suggestions.
  [skipped — this addition targets the Browser SDK subpath (`@anthropic-ai/claude-agent-sdk/browser`), which Claudius does not import. Claudius uses the main SDK server-side; the main SDK's `Options` type already had `promptSuggestions`, and `lib/server/session.ts` has been passing it since commit a5b3793. No new code needed.]

- **0.3.193** — Fixed brief console window flashes on Windows when spawning CLI subprocesses.
  `[skipped — Windows-only platform bug fix in the CLI spawning layer; no SDK API or behavior change, and Claudius targets macOS/Linux.]`

- **0.3.192** — Updated to parity with Claude Code v2.1.192.
  `[skipped — general parity pull with no new public SDK API surfaces exported. The `claudeCodeVersion` in the SDK's `package.json` advanced from 2.1.191 → 2.1.192 (then 2.1.193 in 0.3.193); no Claudius call-sites are affected. Audited: `lib/server/session.ts`, `lib/client/use-session.ts`, `lib/shared/events.ts`, `lib/shared/parse-init.ts`, model routes — none reference CC version numbers in a way that would drift.]`

## Code changes

- No code changes required. The two 0.3.193 changelog items touch only the Browser SDK subpath (which Claudius does not use) and a Windows platform fix. The `promptSuggestions` option that is the substantive new feature of this release was already shipped in Claudius (commit a5b3793) using the same `Options.promptSuggestions` field that was already present in the main SDK's type surface. The CC parity update (0.3.192) added no new public exports that touch Claudius call-sites.

## New UI surfaces

- No new UI surfaces this release. The only substantive SDK change (`promptSuggestions` in the Browser SDK) does not introduce any new UI element — Claudius already renders prompt suggestion chips (PromptSuggestions component) powered by the existing `promptSuggestions: true` option and `prompt_suggestion` SSE message handling, both of which shipped before this release.

## Tests

- No new tests required. No new SDK API surfaces, behavior changes, or code paths were introduced. The existing vitest suite (126 files, 1 386 tests) and Playwright e2e suite exercise the `promptSuggestions` feature path as it stands; no new coverage gaps were opened by this SDK bump.

## Risks / follow-ups

- The CC parity changelog entry for 0.3.192 ("parity with Claude Code v2.1.192") is intentionally terse — it may carry internal TUI or tooling changes not visible as public SDK exports. If a future cc-parity sweep (scripts/cc-parity/) surfaces new flags or behaviours introduced in v2.1.192–v2.1.193, they should be tracked as a separate cc-parity PR rather than retrofit onto this SDK-update PR.
- The Browser SDK (`@anthropic-ai/claude-agent-sdk/browser`) is now shipping a `query()` with `promptSuggestions`, `canUseTool`, hooks, MCP servers, and elicitation callbacks. If Claudius ever adopts the Browser SDK to support a claude.ai-hosted session mode (WebSocket or SSE transport), the `BrowserQueryOptions` type would need to be wired in. Worth tracking if that integration is planned.
