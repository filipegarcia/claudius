# SDK update 0.3.163 → 0.3.165

<!--
  This file is the PRIMARY DELIVERABLE for the SDK-update bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

SDK 0.3.165 (which subsumes 0.3.164) is a pure CLI binary rebuild delivering
parity with Claude Code v2.1.164 and v2.1.165. The diff between the 0.3.163
and 0.3.165 npm tarballs confirms that every TypeScript type file
(`sdk.d.ts`, `sdk-tools.d.ts`, `browser-sdk.d.ts`, `assistant.d.ts`,
`bridge.d.ts`, `extractFromBunfs.d.ts`) is byte-for-byte identical; the only
changes in the package are version numbers in `package.json` and new binary
checksums/sizes in `manifest.json`. Claudius compiles against the SDK's JS/TS
wrapper — which did not change — so no Claudius code changes were required.
All four gates remain green.

## SDK changelog highlights

- v0.3.164 — "Updated to parity with Claude Code v2.1.164"
  `[skipped — binary-only rebuild. npm tarball diff shows zero changes to the
  TypeScript API surface (sdk.d.ts, sdk-tools.d.ts, browser-sdk.d.ts,
  assistant.d.ts, bridge.d.ts, extractFromBunfs.d.ts are identical). Only
  manifest.json binary checksums/sizes and package.json version strings
  changed.]`

- v0.3.165 — "Updated to parity with Claude Code v2.1.165"
  `[skipped — binary-only rebuild. Same finding as 0.3.164: all .d.ts files
  unchanged, manifest.json shows new commit hash
  (7864e1998704fa976b7d630f9ff25815c3f8bf6e, buildDate 2026-06-05T04:39:48Z)
  and updated binary checksums; no JS/TS wrapper changes.]`

## Code changes

- No code changes required. Both 0.3.164 and 0.3.165 are pure CLI binary
  rebuilds with no modifications to the TypeScript API surface that Claudius
  compiles against. The tarball diff between 0.3.163 and 0.3.165 shows six
  .d.ts files unchanged and zero new or removed exports. `bun run build`
  (TypeScript typecheck) confirms no compile errors; `bun run lint` is clean.

## New UI surfaces

- No new UI surfaces this release. Both releases contain only CLI binary
  updates; the TypeScript wrapper layer — and therefore the entire SDK API
  surface that drives Claudius's UI — is unchanged. No new exports, hooks, or
  event types were added, so there are no new browser elements to surface.

## Tests

- vitest: 97 test files, 1009 tests, all passed (0 new specs, 0 updated).
  No Claudius code changed, so no spec additions were needed. The clean run
  confirms all existing coverage continues to hold against SDK 0.3.165.
- playwright: 107 passed, 5 skipped (marketing-screenshot specs, not updated
  by default — see `UPDATE_SCREENSHOTS` flag). 0 new specs, 0 updated. No new
  UI surfaces exist; no spec changes were needed.
- Not covered: the live CLI binary behavior (v2.1.164 / v2.1.165 runtime
  changes, if any) is not exercised in automated tests — those changes are
  internal to the bundled CLI and not visible to the JS/TS host layer.

## Risks / follow-ups

- **Opaque binary changes**: Both changelog entries are identical single-line
  descriptions ("parity with Claude Code v2.1.x") with no specifics. Because
  the TypeScript API is unchanged, any behavioral changes are solely in the
  bundled CLI binary. If Claude Code v2.1.164 or v2.1.165 introduced breaking
  behavior changes (not type-level changes) — e.g. altered tool-result shapes,
  changed session lifecycle behavior, or modified streaming message ordering —
  those would only surface at runtime against a live Claude process, not in the
  typecheck or unit-test gate. The live `chromium-live` e2e project provides
  the only automated guard against this; a manual smoke-test of a new session
  after deploy is the recommended verification path if unusual runtime behavior
  is observed post-merge.
- None of the deferred items from 0.3.163 (optimistic stale-chip pruning,
  Stop hook additionalContext) are affected by these releases.
