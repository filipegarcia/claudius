# SDK update 0.3.251 → 0.3.252

<!--
  The DELIVERABLE is the implemented code change; this file only
  DOCUMENTS it. Writing polished notes is not the job — shipping the
  code the notes describe is. Every claim in "## Code changes" must
  map to a real hunk in `git diff main...HEAD`; notes describing work
  that isn't in the diff is the worst possible outcome (see prompt.md).
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

`0.3.252` is a pure parity bump to Claude Code v2.1.252 with no
Claudius-visible surface change. The upstream changelog is a single
"Updated to parity" line, the orchestrator's `.d.ts` diff is byte-identical
between `0.3.251` and `0.3.252` (zero new declarations), and an independent
`npm pack` diff of both tarballs confirms the only differences are: the
`manifest.json` CLI-binary checksums/sizes for each platform, the
`claudeCodeVersion` string, the `optionalDependencies` version pins, and
internal minified-bundle content in `sdk.mjs`/`bridge.mjs`/`browser-sdk.js`
with unchanged occurrence-counts for every model-identity, pricing, and
capability keyword we checked (`fable`, `modelPicker`, `modelPricing`,
`promptCacheTtl`, `managedSourcesBehavior`, `default_to_no`, `1m`/`[1m]`).
We made no code changes — only the version bump (`package.json`,
`bun.lock`) that the orchestrator already applied. Headline risk: none: a
future release with an actual `.d.ts` diff should not be assumed to be this
quiet just because this one was.

## SDK changelog highlights

- "Updated to parity with Claude Code v2.1.252" — `[skipped — internal CLI
  parity bump, no public API surface changed]`. Verified two ways: (1) the
  orchestrator's `.d.ts` diff between `0.3.251` and `0.3.252` is byte-identical
  ("New declarations" section is empty), and (2) an independent `npm pack`
  diff of both tarballs shows only `manifest.json` binary
  checksums/sizes/`claudeCodeVersion`, `optionalDependencies` version pins,
  and internal minified-bundle bytes in `sdk.mjs` / `bridge.mjs` /
  `browser-sdk.js` — no exported symbol, field, or `package.json`
  `main`/`exports` entry changed.
- **New declarations**: none. The type-surface diff block states
  "no `.d.ts` changes between 0.3.251 and 0.3.252 — the public type surface
  is byte-identical", so there is nothing under this heading to triage.
- **Model-identity audit** (required before any model-related item can be
  skipped, per the second failure mode — done here even though the item
  above is a blanket parity bump, to rule out a silent model-name/pricing
  change hiding behind the one-line changelog): grepped the raw
  `sdk.mjs` bundle in both tarballs for `fable`/`Fable`,
  `modelPicker`, `modelPricing`, `promptCacheTtl`,
  `managedSourcesBehavior`, `default_to_no`, and `[1m]`/`1m context` —
  every occurrence count is identical between `0.3.251` and `0.3.252`.
  Also spot-checked the five model surfaces Claudius hard-codes
  (`app/api/models/route.ts` `STATIC_FALLBACK`, `app/api/models/probe/route.ts`
  pinned-probe list, `app/api/sessions/[id]/model/route.ts` picker rows,
  `lib/client/types.ts` `ModelInfo`, `lib/server/session.ts`
  `enable1mContext`/`context-1m-2025-08-07` gating and the
  fable-advisor-incompatibility clear) — all already reflect the current
  Fable/1M-context state from prior releases and reference nothing that
  changed in this bump. `[skipped — audited, no model-identity change in
  this release]`.

## Code changes

- No code changes required. The SDK's public type surface and exported
  runtime API are byte-identical between `0.3.251` and `0.3.252` (verified
  via the orchestrator's `.d.ts` diff and an independent `npm pack` tarball
  diff — see "SDK changelog highlights" above), and no model-identity,
  pricing, or capability-tier data changed either. There is nothing in
  Claudius (`lib/server/session.ts`, `lib/client/`, `lib/shared/`, the
  model routes, or the UI) that consumes anything this release altered.
  The only diff in this PR relative to `origin/main` is the orchestrator's
  own version bump (`package.json`, `bun.lock`).

## New UI surfaces

- No new UI surfaces this release. The SDK shipped no new/changed public
  API, type, model data, or behaviour for Claudius to expose — there is
  nothing to build a UI for.

## Tests

- No new tests required. There is no new behaviour to cover: the diff
  against `origin/main` is the version bump only, and the existing vitest
  and Playwright suites already exercise every model/session surface this
  audit touched (`session-model-switch.test.ts`, the model-picker and
  cost-overlay specs from prior releases). Ran the full gate suite
  (`bun run lint`, `bun run test`, `bun run build`, `bun run test:e2e`)
  to confirm nothing regressed — see the final gate output in the PR/run
  log.

## Risks / follow-ups

- None identified. This is a vendored-CLI-binary parity bump
  (Claude Code v2.1.251 → v2.1.252) with a byte-identical public type
  surface and no model/pricing/capability data change, verified
  independently of the orchestrator's diff via an `npm pack` tarball
  comparison. The one thing worth flagging for the next run: don't
  assume future "parity" one-liners are this quiet — this release
  happened to have zero `.d.ts` movement, but that must be re-verified
  each time, not inferred from this precedent.
