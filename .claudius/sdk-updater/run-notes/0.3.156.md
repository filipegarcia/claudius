# SDK update 0.3.154 → 0.3.156

<!--
  This file is the PRIMARY DELIVERABLE for the SDK-update bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

Both 0.3.155 and 0.3.156 are pure binary/runtime parity bumps with no public
SDK surface change. Verified by diffing all four type-declaration files
(`sdk.d.ts`, `agentSdkTypes.d.ts`, `bridge.d.ts`, `sdk-tools.d.ts`) between
the installed 0.3.154 and 0.3.156 packages — zero lines differ. The bundled
Claude Code CLI binary advanced from v2.1.154 → v2.1.156 (commit
`de3d672b5e8c35ae78d81c9dd83844d334ec63af`, built 2026-05-28T18:38:44Z);
Claudius inherits whatever internal runtime fixes shipped in v2.1.155 and
v2.1.156 with no code changes of its own. The headline risk to flag is the
pre-existing `bun run build` failure on `/_global-error` (Next.js 16 prerender
bug, digest `1753362301`) that was present on `origin/main` before this bump
and remains unchanged.

## SDK changelog highlights

- `0.3.155` — "Updated to parity with Claude Code v2.1.155":
  `[skipped — parity bump; all four `.d.ts` files are byte-for-byte identical
  to 0.3.154. No public SDK export changed. Claudius benefits passively from
  any internal runtime improvements.]`

- `0.3.156` — "Updated to parity with Claude Code v2.1.156":
  `[skipped — parity bump; same evidence as above. Binary checksum changed
  (new build of the embedded Claude Code CLI), types unchanged.]`

Evidence: `diff /tmp/sdk-compare/node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts
./node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts` produced no output,
confirming zero type-surface change across all declaration files.

## Code changes

- No code changes required. The 0.3.154 → 0.3.156 upgrade ships only an
  updated Claude Code CLI binary inside the npm package; the TypeScript
  declaration files (`sdk.d.ts`, `agentSdkTypes.d.ts`, `bridge.d.ts`,
  `sdk-tools.d.ts`) are identical between versions, leaving Claudius's
  existing SDK usage entirely valid. No imports, type annotations, or runtime
  call-sites needed to change.

## New UI surfaces

- No new UI surfaces this release. The SDK upgrade adds no new public exports,
  message types, or callback shapes, so there is nothing new to surface in the
  Claudius browser UI.

## Tests

- vitest: 0 new specs, 0 updated — 56 files / 602 tests all passed, unchanged
  from the pre-bump baseline.
- playwright: 0 new specs, 0 updated — 92 passed, 5 skipped (same as
  pre-bump baseline). Not covered: any behaviour gated on the unreleased
  v2.1.155/v2.1.156 internal changes (no public API surface to exercise).

## Risks / follow-ups

- **Pre-existing build failure (unrelated to SDK bump):** `bun run build` fails
  during prerender of `/_global-error` with
  `TypeError: Cannot read properties of null (reading 'useContext')`
  (digest `1753362301`, Next.js 16 internals). This bug was already documented
  in the 0.3.154 run-notes (digest `204178855` at that time — same root cause,
  different digest across builds) and exists on `origin/main` independently of
  this upgrade. This is a Next.js 16 static-prerender bug, not a Claudius or
  SDK-upgrade issue; it should be tracked and fixed separately.
- lint, vitest, and test:e2e all pass cleanly with zero SDK-related failures.
