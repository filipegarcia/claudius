# Claude Code parity 2.1.215 → 2.1.216

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

2.1.216 is a mostly-maintenance release: one settings.json addition
(`sandbox.filesystem.disabled`) and ~50 bug fixes, almost all of them
tied to CLI-only surfaces (terminal rendering, daemon/background-agent
lifecycle, key chords, PowerShell/Windows quirks) that have no Claudius
analog. Two changelog lines described behavior Claudius was *not*
actually at parity on despite already having the surrounding feature
(`/context` exceeding the window, a failed `/compact`), so this release
also closes those two small gaps. Bucket B this release: (1) a new
"Skip filesystem isolation" checkbox nested under the existing Sandbox
toggle in `WorkspaceForm.tsx`, (2) an explicit "Context window exceeded"
banner state (shown unconditionally, even past the user's "Never nudge
me" threshold pref), and (3) a visible "Compaction failed: …" error
when a `/compact` turn ends without producing a `compact_boundary`.
Headline risk to flag for review: for (3), Claudius infers "the compact
failed" from the *absence* of a `compact_boundary` on the very next
result rather than from an SDK-native "this was a compaction turn and it
failed" signal — the SDK doesn't expose the latter. An adversarial
self-review pass (see Risks) caught two real bugs in the first version
of this heuristic (an SSE-reconnect replay could dangle the tracking
flag and misattribute a later, unrelated turn's failure; a `/compact`
queued behind an already-running turn via `sendQueuedNow`/the `asap`
dispatch mode could do the same) — both are fixed, and the same pass
also caught a rounding bug in the context-exceeded banner (a raw 99.5%
was displaying as "100% exceeded"). All are fixed and covered by new
tests; see Risks for the residual, deliberately-accepted scope
narrowing.

## Changelog classification

- `[B — reimplement in Claudius] Added sandbox.filesystem.disabled setting to skip filesystem isolation while keeping network egress control` — a genuine new settings.json leaf (confirmed present in the installed SDK's `SandboxSettingsSchema.filesystem.disabled`, `sdk.d.ts:2707`). Claudius already has a bespoke (non-generic-catalog) "Sandbox shell commands" toggle in `WorkspaceForm.tsx` mapping to `Options.sandbox.enabled` — this is the matching granular leaf under it, not something that rides through automatically since Claudius's sandbox UI is hand-rolled, not a `SDK_SETTINGS_CATALOG` passthrough. Shipped (see below).
- `[B — reimplement in Claudius] "/context" should show an explicit warning when the conversation exceeds the context window, and a failed "/compact" should display as an error` — thin changelog line, disambiguated by reading Claudius's own code: `ContextWarningBanner.tsx`/`useContextWarning.ts` already existed but only had a 95%-vs-under-95% amber/red tone split (no distinct "exceeded" copy/state), and a failed `/compact` silently reverted the "Compacting…" indicator with zero visible error (confirmed by tracing `ChatSurface.tsx`'s own comment: *"fall back to a pending true→false edge (compaction errored / no-op)"*). Both are small, idiomatic gaps in an existing surface, not new features — shipped (see below).
- `[A — already via SDK updater] "/rewind" no longer restores or deletes files through symlinks or hard links at tracked paths and reports how many paths it skipped` — matches the `RewindFilesResult.skippedLinks` field the SDK migration on this branch (0.3.215→0.3.216) already wired end-to-end (`components/chat/RewindFilesButton.tsx`, per `.claudius/sdk-updater/run-notes/0.3.216.md`). Same underlying safety-guard mechanism; not re-implemented here per the branch instructions.
- `[A — already via SDK updater] Fixed AskUserQuestion telling Claude to continue even when your answer asked it to wait or explain first` — this is model/engine-side wording logic for how the SDK's AskUserQuestion tool interprets a free-text answer; Claudius only renders the tool's UI and forwards the user's literal answer text — the interpretation change is entirely on the SDK/engine side.
- `[A — already via SDK updater] Fixed resumed background agent sessions reverting to the default agent (prompt/tool restrictions restored)` — session-resume/agent-restore behavior lives in the SDK's session-management internals (`Options.agent` resolution on resume), not in anything Claudius owns.
- `[A — already via SDK updater] Fixed worktree-isolated subagents redirecting git into the shared checkout via git -C/--git-dir/GIT_DIR/GIT_WORK_TREE` — engine-side sandboxing of the Bash tool's git invocations.
- `[A — already via SDK updater] Fixed Bash command permission checking for compound statements with redirects inside && lists or negations` — the permission-rule evaluator for Bash command strings is SDK-internal; Claudius only consumes `canUseTool` decisions.
- `[A — already via SDK updater] Fixed MCP re-authenticate revoking working credentials before the new sign-in succeeds` — MCP OAuth flow internals live in the SDK's MCP client.
- `[A — already via SDK updater] Fixed read-only commands on Windows accessing network paths without a permission prompt` — engine-side permission-prompt gating logic.
- `[A — already via SDK updater] Fixed skills/commands changed during a session not appearing in the slash menu until restart; plugin skills with a name frontmatter field losing their plugin prefix in autocomplete` — both are the SDK's own live-reload/autocomplete-merge logic for `system:init`/`supportedCommands()`, which Claudius's `mergeSuggestions()` (`lib/shared/slash-commands.ts`) already just consumes verbatim.
- `[A — already via SDK updater] Fixed telemetry misreporting permission denials (failed prompts no longer counted as rejections; interrupts reported as aborts)` — the SDK's own telemetry pipeline; Claudius doesn't have a parallel telemetry system to fix in step.
- `[A — already via SDK updater] Updated the bundled dataviz skill (palette reorder, four-series guidance fix)` — the dataviz skill ships inside the SDK's bundled-skill set; Claudius has no bundled-skill scope of its own (`lib/server/skills.ts` only models `user`/`project`), so the content update flows through with the SDK bump, nothing to touch. (Separately, Claudius still doesn't expose the SDK's `disableBundledSkills` toggle at all — a pre-existing gap from an earlier release, out of scope here; see Risks.)
- `[C — CLI/terminal only] Background sessions: /mcp and /install-github-app now park a "needs input" request in the "agent view" when no client is attached; Fixed claude daemon stop --any; Fixed background subagent sessions reverting to default agent on resume; Fixed background sessions whose worktree has no git repo being undeletable; Fixed Ctrl+X twice in the agent list failing to delete a session` — this whole cluster describes Claude Code's terminal daemon model: agent sessions that keep running detached from any terminal, manageable via a TUI "agent list" and re-attachable with `claude attach <id>`. Claudius's session model is architecturally different — sessions are server-persistent but reaped shortly after the last subscriber disconnects (`SessionManager`'s subscriber-count-driven grace-window reap in `session-manager.ts`), so there's no long-lived "detached background session" to attach to, and no "agent view" TUI to add a badge to. The closest existing Claudius mechanism (the MCP-needs-auth `systemEntries` pill, fires to whichever client next subscribes) already covers the underlying "surface a pending action" need within that lifecycle. See Risks for the maximal alternative considered and rejected.
- `[C — CLI/terminal only] Fixed Esc-Esc at an idle prompt not opening the rewind picker in long-running sessions with background tasks` — terminal key-chord behavior.
- `[C — CLI/terminal only] Fixed mouse/focus garbage while a GUI editor from /memory, /plan, /keybindings, or Ctrl+G is open; /memory no longer waits for the editor to close` — terminal ANSI/mouse-mode handling around external `$EDITOR` launches; Claudius's `/memory` etc. never shell out to a terminal editor.
- `[C — CLI/terminal only] Fixed dialogs in fullscreen mode stretching past the panel edge; /config settings list clipping its footer; transcript-mode (Ctrl+O) footer hint wrapping under 104 columns` — terminal-UI layout bugs in the CLI's own renderer.
- `[C — CLI/terminal only] Fixed the Prometheus metrics endpoint emitting invalid # UNIT lines` — OTEL/observability config for the CLI process, not something a browser app exposes.
- `[C — CLI/terminal only] Fixed non-ASCII Bash command parsing and PowerShell tool argument validation of invisible Unicode; improved git/gh argument validation in the PowerShell tool` — Windows/PowerShell-specific tool internals.
- `[C — CLI/terminal only, different product] Fixed Claude Code on the web re-asking the same question and dropping the answer after idle; fixed cloud sessions dropping the in-flight message on container restart; fixed Claude-in-Chrome 403-looping on reconnect` — all three describe Anthropic's own hosted products (Claude Code on the web, cloud-container sessions, the Claude-in-Chrome browser extension), not Claudius, which runs its own server and SSE session model end-to-end.
- `[C — CLI/terminal only] [VSCode] Fixed RTL text rendering order` — the VSCode extension is a different codebase/product from Claudius.
- `[C — CLI/terminal only] Improved the /fork confirmation to one line with the session name, claude attach id, and a shared-checkout note` — Claudius's Fork is a full dialog-driven UI flow (`UserMessage.tsx`, `ChatSurface.tsx`), not a one-line terminal confirmation; there's no `claude attach` id concept to show.
- `[A — already via SDK updater] Improved the /ultrareview diff-too-large error and /code-review ultra empty-diff message` — both `/ultrareview` and (implicitly) the CLI's own review commands are registered in `lib/shared/slash-commands.ts` with `handler: "sdk"` — Claudius forwards the literal command text and renders whatever the SDK/CLI returns verbatim, so an improved error string arrives with zero Claudius-side change.
- `[skip — ambiguous, see Risks] Improved the spend limit adjustment prompt to show the server's rejection reason` — Claudius has no admin/org spend-limit-policy adjustment flow at all (its own budget caps are a local per-workspace/session `maxBudgetUsd`, a different concept from an org-tier spend-limit policy enforced server-side); building one now would be inventing scope the changelog entry doesn't clearly ask for. See Risks.
- `[C — CLI/terminal only, not applicable] Fixed workflow saves and scheduled-task writes following a symlink at .claude` — a symlink-safety fix in Claude Code's own local workflow/schedule file persistence. Claudius has no equivalent feature that writes user-named files under a `.claude`-style directory from request-controlled input (grepped for a Claudius-owned "workflows" file-persistence surface — none exists; the "Workflow" tool in this very pipeline is an orchestration tool, not a Claudius product feature). No action needed, but see Risks for the general path-safety note.

Pure mechanical/UI bug fixes not itemized above (message-normalization
slowdown, HTTP-401 auto-mode denial, @-mention/vim/statusline/resume-picker
fixes, etc.) are elided per the "pure bug-fix entries can be elided" rule —
all are engine-side or terminal-only with no Claudius surface to touch.

## Implemented (bucket B)

- `[extend: Workspace settings (WorkspaceForm.tsx)] sandbox.filesystem.disabled` — added a "Skip filesystem isolation" checkbox nested under the existing "Sandbox shell commands" checkbox (visible only while the parent is checked, since the SDK leaf is meaningless without `sandbox.enabled`). Threaded end-to-end: `lib/server/workspaces-store.ts` (`WorkspaceDefaults.sandboxFilesystemDisabled`), `lib/shared/session-defaults.ts` (merge precedence), `lib/shared/events.ts` (`CreateSessionRequest`), `app/api/sessions/route.ts`, `lib/server/session.ts` (field, ctor, `getRebuildOpts`, forwarded as nested `sandbox.filesystem.disabled: true` alongside `sandbox.enabled: true`), `components/workspaces/WorkspaceForm.tsx`. Rejected alternative: a top-level "Advanced sandbox" sub-panel — overkill for one boolean leaf; nesting it directly under the existing checkbox mirrors the SDK's own nesting and Claudius's existing "sub-option only shown when the parent is on" idiom used nowhere else yet but consistent with the checkbox-list layout already there.
- `[extend: Chat context-warning banner (ContextWarningBanner.tsx)] Explicit "context window exceeded" state` — `lib/client/useContextWarning.ts`'s `shouldShowContextWarning` now always returns `true` once usage is genuinely over 100%, regardless of the user's threshold preference (even "Never"); `ContextWarningBanner.tsx` renders distinct "Context window exceeded — N% full" copy (vs. the existing "Context window is N% full" nudge) for that state. Rejected alternative: only changing the `/context` overlay (`ContextOverlay.tsx`) since that's literally where the changelog line says the warning shows — rejected because the banner is Claudius's actually-actionable, always-visible surface (the overlay is opt-in/on-demand), and the CLI's own behavior is described as "showing a warning", which the banner already does the job of for every other threshold; only the "exceeded" tier was missing there.
- `[extend: Chat message list error banner (session.errors → data-testid="session-error")] "Compaction failed" error` — `lib/client/use-session.ts` tracks a pending `/compact` via the `slash_invoked` breadcrumb (`pendingCompactRef`); if the very next `result` event carries a non-success subtype with no intervening `compact_boundary`, it pushes `Compaction failed: <detail>` onto the existing `session.errors` array (already rendered by `ChatSurface.tsx` as a red bordered banner — no new visual language). Rejected alternative: a dedicated `SystemPill`-style pill (mirroring `HookFailurePill`) inline in the transcript at the point of the `/compact` invocation — rejected as more code for the same visual outcome; the generic `session.errors` banner already exists precisely for "a turn ended badly, tell the user" (it's where `error_max_budget_usd` already surfaces), so this is the smaller, more idiomatic diff.

## New UI surfaces

- **"Skip filesystem isolation" nested checkbox** in the New/Edit Workspace form's Advanced section, shown once "Sandbox shell commands" is checked. Screenshot: `docs/cc-parity/2.1.216/sandbox-filesystem-disabled.png`. Captured by `tests/e2e/cc-parity-2.1.216-sandbox-filesystem-disabled.spec.ts`. Context: the "New workspace" modal open over the dimmed workspace rail, Advanced section expanded, both sandbox checkboxes checked.
- **"Context window exceeded" banner** above the composer, replacing the normal "Context window is N% full" copy once usage passes 100%. Screenshot: `docs/cc-parity/2.1.216/context-window-exceeded.png`. Captured by `tests/e2e/cc-parity-2.1.216-context-and-compact.spec.ts`. Context: a live chat page (StatusLine, right-rail Activity panel, composer all visible) with a mocked 142%-full context reading.
- **"Compaction failed: …" error banner** below the "Running /compact…" system pill, when a `/compact` turn ends in an error result with no `compact_boundary`. Screenshot: `docs/cc-parity/2.1.216/compact-failed.png`. Captured by the same spec file (`tests/e2e/cc-parity-2.1.216-context-and-compact.spec.ts`). Context: same chat chrome, mocked SSE stream delivering a `slash_invoked`(`/compact`) event followed directly by an `error_during_execution` result.

## Tests

- vitest: 2 new specs (`tests/unit/context-warning.test.ts` — `shouldShowContextWarning`'s ≥100%-override behavior, 4 cases; `tests/unit/session-defaults.test.ts` extended with a `sandboxFilesystemDisabled` precedence case), 1 file updated.
- playwright: 2 new specs, 5 total test cases: `cc-parity-2.1.216-sandbox-filesystem-disabled.spec.ts` (1 case) and `cc-parity-2.1.216-context-and-compact.spec.ts` (4 cases — the exceeded banner + screenshot, the 99.5%-boundary regression guard added after the adversarial review caught the rounding bug, the compaction-failed banner + screenshot, and the error_max_budget_usd double-banner regression guard).
- Not covered: (1) the actual SDK `Options.sandbox` object construction inside `Session` (the nested `{ filesystem: { disabled: true } }` shape) has no dedicated unit test — matches the existing precedent for `sandboxEnabled` itself, covered only at the merge-logic layer and the UI/API layer, not inside `Session`'s private options-building code. (2) The two edge cases the adversarial review found in the compact-failed heuristic (SSE-reconnect replay dangling the tracking ref; a `/compact` queued behind an in-flight turn) are fixed in the code but not covered by a dedicated e2e — simulating an EventSource reconnect mid-test and simulating `sendQueuedNow`/`asap`-mode queuing both need more test-harness scaffolding than this release's scope justified; flagged as a follow-up below.

## Risks / follow-ups

- **`[B] sandbox.filesystem.disabled`**: alternative considered — exposing it as a raw `SDK_SETTINGS_CATALOG` entry (the generic settings.json passthrough pattern used for flatter booleans like `promptSuggestionEnabled`) instead of a bespoke workspace-form checkbox. Rejected because Claudius's existing sandbox toggle (`sandboxEnabled`) is *already* a bespoke per-workspace default, not a catalog entry — nesting the new leaf under the catalog would have split one logical feature (sandbox config) across two different settings UIs (the Workspace form and the generic Settings page), which would be more confusing than consistent with the precedent that already exists. An adversarial review pass also flagged that this codebase has no other "child field only shown when parent is on" precedent (the one comparable case, `app/settings/page.tsx`'s status-line refresh interval, uses "visible but disabled" instead of "hidden") — noted as a new UI convention this commit inaugurates, not a defect.
- **`[B] Context window exceeded`**: the >100% override in `shouldShowContextWarning` intentionally overrides the user's "Never" (threshold=100) preference — the direct precedent (`useRateLimitWarning.ts`'s "Hard stop only" preset, which similarly always shows on outright rejection regardless of threshold) makes this a consistent product call, not a one-off exception. The "Never" preset's description was updated to say so explicitly (an adversarial review pass caught that the old copy — "Hide the context warning entirely" — would have been a flat lie post-change). The same pass also caught a rounding bug: the banner's own `exceeded` boolean was computed from `Math.round(percentage) >= 100`, which mislabeled a genuine 99.5% as "100% exceeded"; fixed to compare the raw percentage (`percentage > 100`), matching the predicate's own boundary, with an e2e regression guard at the 99.5% boundary.
- **`[B] Compaction failed`**: the failure signal is inferred from "no `compact_boundary` landed before the next `result`", not from an SDK-native "this turn was a compaction and it failed" field (no such field exists in the installed SDK's `SDKResultError`/`SDKResultSuccess` types). An adversarial review pass found this heuristic's original "only one turn is ever in flight" assumption doesn't universally hold and produced two real bugs, both now fixed: (1) an SSE-reconnect tail-replay could re-arm the tracking ref for an already-resolved compact (no per-event dedup), later misattributing an unrelated turn's failure — fixed by deduping the arming `slash_invoked` event by uuid; (2) a `/compact` queued behind an already-running turn (`sendQueuedNow`, or the opt-in `asap` queue-dispatch mode) breaks the "next result belongs to this compact" assumption — fixed by only arming the tracker when no other turn is already in flight (`!pendingRef.current` at arm time), which covers every UI-driven compact (both Compact buttons already refuse to fire mid-turn) and simply skips tracking — silently reverting as before, not misattributing — for the rarer manually-typed-mid-turn case. Also fixed: a `/compact` failing with `error_max_budget_usd` no longer double-banners (that subtype already has its own clearer message). Residual, deliberately-accepted gap: a `/compact` sent while genuinely mid-turn still gets no failure banner at all (silent revert, matching pre-release behavior) — closing that fully would need the SDK to correlate a `result` back to the `user_message_uuid` that triggered it even on the error path, which `SDKResultError` doesn't carry today (only `SDKResultSuccess` does).
- **`[skip — ambiguous] Spend limit adjustment prompt`**: disambiguation attempt was reading Claudius's own cost/budget code (`components/cost/`, `LimitsPanel.tsx`) — found only local per-workspace/session USD caps (`maxBudgetUsd`), no org/admin spend-limit-policy concept at all. The changelog line implies a server-enforced org policy rejecting a user's requested limit increase, which has no Claudius equivalent to extend. Flagging in case a future release clarifies this is meant to apply to Claudius's own budget caps (e.g. a workspace cap increase getting rejected by some policy) — nothing like that exists today, so there was nothing to extend.
- **`[C] Background sessions "needs input" in agent view`**: the maximal alternative considered was building a persistent, cross-session "needs input" badge on the sessions list (`app/[workspaceId]/sessions/page.tsx`), modeled on the existing `McpNeedsAuthNoticeEvent` → `use-session.ts` → `systemEntries` pipeline but promoted to a sessions-list-level indicator. Rejected as out of scope for this release: Claudius's session lifecycle reaps a session shortly after the last subscriber disconnects (no indefinitely-running "detached" sessions the way Claude Code's daemon model has), so the underlying "a background agent needs your attention while you're not looking" scenario this CC feature addresses doesn't really arise in Claudius's architecture today. Worth reconsidering if Claudius ever grows a true long-lived detached-session concept.
- **Pre-existing gap, out of scope**: `disableBundledSkills` (from an earlier CC release, tracked in `lib/server/claude-code-changelog.ts:43`) is still not exposed anywhere in Claudius (confirmed absent from `SDK_SETTINGS_CATALOG`). Not part of this release's changelog, not touched here, but noted since this release's dataviz-skill-update item brushes the same "bundled skills" area.
- **`.claude` symlink-write safety**: the workflow/scheduled-task symlink fix (elided above as CLI-only/not-applicable) is a reminder that any *future* Claudius feature writing user-named files under a `.claude`-style directory from request-controlled input should follow the existing `CLAUDE.md` path-safety conventions (`path.resolve` + inline `startsWith` check at the sink) — no such feature exists today, so no action was needed this release.
