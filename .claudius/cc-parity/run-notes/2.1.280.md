# Claude Code parity 2.1.278 → 2.1.280

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

2.1.280 is dominated by TUI dialog polish, VSCode-extension parity, and
cloud (Claude Code on the web / Claude Tag / self-hosted runner) items that
have no Claudius (browser app) surface. Three real bucket-B items survive
the audit and are shipped this release: (1) Claudius's own curated
model-probe list (`app/api/models/probe/route.ts`) needed the new
`claude-opus-5-5` pin, the same treatment the 2.1.257 Fable 5.1 release
got — this is a real gap the first classification pass missed by only
checking the live `/model` picker (which does get Opus 5.5 for free from
the SDK) and not the separate hand-maintained probe list; (2) the `@`
file-mention picker's ranking fix (a filename match now outranks a
folder-only match), matching upstream exactly since Claudius owns this
ranking logic itself, not the SDK; (3) Claudius's `/skills` overlay,
previously a bare name list, now shows each skill's source and token
estimate (reusing the same `/skill-doctor` context data) plus a
click-to-toggle on/off state backed by the SDK's `Settings.skillOverrides`
key — confirmed present in the exact SDK version (0.3.278) this repo's
lockfile pins, not just a newer version that happened to be sitting in
`node_modules` from an unrelated concurrent process. Headline risks to
flag for review: the skills toggle only exercises the binary "on"/"off"
pair of the SDK's four-state `skillOverrides` value (see Risks), and the
first classification pass initially wrote off *every* `[VSCode]`
changelog bullet as not-applicable, which was wrong for the skills-dialog
entry — reviewers should double-check the remaining `[VSCode]` bullets
below read correctly.

## Changelog classification

- `[A — already via SDK updater] Added Claude Opus 5.5 (claude-opus-5-5), now the default Opus model` — the live `/model` picker sources models from the SDK's `Query.supportedModels()` at runtime, so the alias-resolved picker needs no Claudius change. *However* — see bucket B below — Claudius separately hand-maintains a pinned-model-id probe list that isn't SDK-driven and did need the new id.
- `[B — reimplement in Claudius] Added Claude Opus 5.5 (claude-opus-5-5)` (the probe-list half) — `app/api/models/probe/route.ts`'s `PROBE_CANDIDATES` is Claudius's own curated list of full model IDs it live-probes for availability; it's not sourced from the SDK and needed the same manual pin the 2.1.257 Fable 5.1 release got (`git show 61da5d0`). Shipped.
- `[A — already via SDK updater] Changed the default model on Pro and Team Standard plans from Sonnet to Opus` — an Anthropic account/plan-tier default resolved server-side by the API; Claudius has no hardcoded default model of its own to update (every surface either defers to the account default or reads `supportedModels()` live).
- `[B — reimplement in Claudius] Improved @ file suggestions: a file whose name contains the query now ranks above one that only matches across its folder names` — Claudius owns this ranking logic itself (`components/chat/AtMentionPicker.tsx` → `/api/fs/list` → `lib/server/fs-list.ts`'s `listFs()`, not the SDK). The existing scorer ranked purely on full-`relPath` substring position with no distinction for *where* a match landed. Shipped: a basename-match bonus tier.
- `[C — CLI/terminal only] Added mouse support to more lists in fullscreen mode (wheel scroll /skills, clickable /plugin state)` — fullscreen-mode terminal mouse handling; native browser scroll/click already work in Claudius's own `/skills` overlay and `/plugins` page.
- `[A — already via SDK updater] Added CLAUDE_CODE_MAX_MCP_DESCRIPTION_LENGTH` — Claudius has no MCP-description truncation of its own. Claudius's session spawn (`buildQueryEnv` in `lib/server/session.ts`) falls through to a full copy of `process.env` when there's no per-account env override, so this env var reaches the bundled CLI subprocess unmodified if an operator sets it on the Claudius server process — no product surface needed.
- `[A — already via SDK updater] Added hook output sizes / oversized-output count to the hook_execution_complete OTel event` — Claudius doesn't run its own OpenTelemetry exporter for hook execution; this is bundled-CLI-internal telemetry enrichment with no Claudius consumer.
- `[C — CLI/terminal only] Reverted ctrl+l / cmd+k clearing the transcript in fullscreen mode` — a terminal keybinding revert; Claudius has its own independent keybinding system (`/keybindings`), unrelated to this TUI shortcut.
- `[C — CLI/terminal only] Improved /permissions focus-return, tab nav, delete/remove-directory confirmations defaulting to No` — describes the CLI's own fullscreen TUI rule-list widget (focus rings, tab bar). Claudius's `/permissions` (`app/[workspaceId]/permissions/page.tsx`) is a native React panel with its own interaction model, not a port of the CLI's TUI.
- `[C — CLI/terminal only] Improved /cost cache-miss causes naming thinking mode changes / /install-github-app "Esc to cancel" / /artifacts+/workflows scrollbar / workflow progress tree dim dot / /plugin Add Marketplace box alignment / /workflows fullscreen divider / code blocks without a language colored like inline code` — TUI chrome/copy polish with no browser analog. On the code-block item specifically: Claudius's `CodeBlock.tsx` already gives every fenced block — labeled or not — full chrome (border, copy button, Shiki highlight even with `lang=undefined`), so the readability problem this upstream fix targets doesn't exist in Claudius's renderer.
- `[A — already via SDK updater] Improved /btw asked while a tool is still running: the side question now knows that call is in progress` — this is the product-visible effect of the SDK's in-flight-turn side-question handling; Claudius's `/btw` forwards literal text through the normal streamed-input path with no direct call site of its own to update, so the fix is inherited automatically once the SDK bumps.
- `[A — already via SDK updater] Improved the UserPromptSubmit hook timeout notice and debug log to name which hook command timed out` — the notice/log line is composed and emitted by the bundled CLI subprocess; Claudius's hooks UI just displays whatever the CLI streams.
- `[C — CLI/terminal only] Changed /autocompact footer hint to name ←/→, /fast footer to name Space` — TUI footer-hint copy; Claudius's `/autocompact` and `/fast` settings are plain React controls with their own affordance copy, not a footer hint to keep in sync.
- `[A — already via SDK updater] Changed effort-level persistence for newly released models and stopped Opus 4.7/4.8/Fable 5 overriding /effort` — both are the SDK's own effort-resolution precedence logic at request time; Claudius only ever forwards the user's chosen effort level and never reimplements the CLI's internal default-vs-saved-level arbitration.
- `[A — already via SDK updater] Changed plugin marketplaces whose name imitates a reserved marketplace name to be refused` / `Changed PermissionRequest hooks: an agent-type hook no longer runs there` — Claudius's plugin page explicitly delegates marketplace add/resolution/download to "the SDK's slash command" (`app/plugins/page.tsx:207`) rather than reimplementing it, and Claudius's hooks editor persists arbitrary hook configs without pre-validating type/event combinations — both guards run entirely inside the engine.
- `[skip — already shipped in an earlier cc-parity release] [VSCode] Added a Status dialog … /status` — Claudius already has `/status` (`lib/shared/slash-commands.ts:214`).
- `[skip — already shipped in an earlier cc-parity release] [VSCode] Added a Sandbox dialog … /sandbox` — already has `/sandbox` (`lib/shared/slash-commands.ts:88`).
- `[skip — N/A] [VSCode] Added a Claude in Chrome dialog … /chrome` — Claudius's `/chrome` is already an explicit no-op ("Claudius runs in Chromium", `lib/shared/slash-commands.ts:187`).
- `[skip — already shipped in an earlier cc-parity release] [VSCode] Added Export conversation … /export` — already has `/export` (`lib/shared/slash-commands.ts:81`).
- `[B — reimplement in Claudius] [VSCode] Added each skill's source, token estimate and on/off state to the Slash commands dialog, with a click to change the state, and a typed /skills that opens it` — the first classification pass wrote this off as "VSCode-only, not applicable" without checking whether Claudius's own `/skills` overlay could be extended the same way `/status`/`/sandbox`/`/export`/`/plan` already were in earlier cycles. It could: Claudius's `SkillsOverlay.tsx` already opens via `/skills` but only listed bare names. Shipped: source + token estimate (reused from the existing `/skill-doctor` context data) and a click-to-toggle on/off state backed by the SDK's `Settings.skillOverrides` key (confirmed present in the exact 0.3.278 SDK this repo's lockfile pins, via `https://unpkg.com/@anthropic-ai/claude-agent-sdk@0.3.278/sdk.d.ts`).
- `[skip — already shipped in an earlier cc-parity release] [VSCode] Added a typed /plan …` — already has `/plan` (`lib/shared/slash-commands.ts:251`).
- `[skip — ambiguous, see Risks] [VSCode] Improved pasted-text handling (paste-over-800-chars marking) / invisible-Unicode-formatting stripping` — both require composer-level paste-span tracking for plain-text paste that `PromptInput.tsx`'s `onPaste` doesn't currently do (it only intercepts file/image clipboard items). Conservative call: leave alongside this cycle rather than build a half-featured version; see Risks for the maximal alternative.
- `[skip — CLI/terminal only] [VSCode] Changed "Open in New Tab" to open beside the current editor group` — VS Code editor-group layout; no editor-group concept in a standalone browser app.
- `[skip — N/A, different host product] [Claude Code on the web] *` (admin Routines setting relocation, GHE token renewal, routine resume-with-stale-prompt fix, disabled cross-session file links, auto mode retry fix, Claude-app file saving) — all describe claude.ai-hosted cloud sessions; Claudius is a self-hosted wrapper around a local CLI subprocess, not a claude.ai cloud session host.
- `[skip — N/A, different host product] [Claude Tag] *` (Slack Working indicator/Stop button, guest-join notice, scheduled-routine fix, file-scan retry, bullet-list rendering fix, GitHub banner reasons) — Slack app integration, no Claudius surface.
- `[skip — N/A, different host product] [Code Review] Improved the Code Review check run …` — Anthropic's hosted GitHub Code Review bot; not a Claudius surface.
- `[skip — N/A, deployment mode] Self-hosted runner: * / Windows: *` — deployment modes Claudius doesn't ship (the managed self-hosted-runner product; Windows-only cleanup-path fixes for a different install layout).

Pure bug-fix entries (the majority of this release) are elided per the
instructions.

## Implemented (bucket B)

- `[extend: model picker "more models" probe list] Opus 5.5 pin` — added `claude-opus-5-5` to `PROBE_CANDIDATES` in `app/api/models/probe/route.ts`, same shape as every other pinned generation (description, 1M context note). Files touched: `app/api/models/probe/route.ts`. Rejected alternative: none — this is a data-row addition to an existing curated list, not a new surface; the live `/model` picker itself needs no change since it already reads `supportedModels()`.
- `[extend: chat composer @-mention picker] @ file-suggestion ranking — filename matches now outrank folder-only matches` — `lib/server/fs-list.ts`'s `listFs()` scorer now adds a ranking bonus for a basename substring match. Files touched: `lib/server/fs-list.ts`, `tests/unit/fs-list-filename-ranking.test.ts`, `tests/e2e/cc-parity-2.1.280-at-mention-filename-ranking.spec.ts`. Rejected alternative: a new dedicated "file search" screen — overkill; this is a pure ranking refinement to an existing, already-shipped affordance.
- `[extend: /skills overlay] Skill source, token estimate, on/off toggle` — extended `components/overlays/SkillsOverlay.tsx` to fetch and merge per-skill `source`/`tokens` from the existing `/api/sessions/[id]/context` endpoint (the same data `/skill-doctor`'s `ContextOverlay` already renders — no new server-side computation for that half) and added a click-to-toggle backed by a new `skillOverrides` settings key. Files touched: `lib/server/settings.ts` (`ClaudeSettings.skillOverrides`, `isSkillOverrideValue`, `updateSkillOverride`), `app/api/settings/skill-overrides/route.ts` (new — GET/POST), `components/overlays/SkillsOverlay.tsx`, `components/chat/ChatSurface.tsx` (threaded `sessionId`/`cwd` props). Rejected alternative: a `[new screen: /skills-catalog]` dedicated page — overkill; Claudius already has a *separate*, purpose-built skills-authoring page (`app/[workspaceId]/skills`) for editing skill files, and this dialog is a variation on the existing lightweight session-scoped `/skills` overlay, not a new domain.

## New UI surfaces

- `docs/cc-parity/2.1.280/at-mention-filename-ranking.png` — `tests/e2e/cc-parity-2.1.280-at-mention-filename-ranking.spec.ts`. Captured mid-session in the full chat chrome (side nav, session tab strip, Activity panel) with `@config` typed in the composer, against a disposable temp-dir workspace built with a deliberately discriminating fixture (`deep/nested/dir/config.ts`, a filename match, ranked above `config-utils/` + `config-utils/index.ts`, folder-only matches) so the screenshot itself proves the fix.
- `docs/cc-parity/2.1.280/skills-overlay-toggle.png` — `tests/e2e/cc-parity-2.1.280-skills-toggle.spec.ts`. Captured with the `/skills` overlay open over the chat surface (composer + rail chrome visible behind the dimmed backdrop), showing `pdf-fill` (project · 1.2k, toggled off — dim circle, strikethrough) and `code-review` (user · 340, on — filled green dot) mid-test, as part of an off→on round trip the spec asserts against the real `.claude/settings.json` file it wrote.

## Tests

- vitest: 2 new specs (10 test cases total), 0 updated.
  - `tests/unit/fs-list-filename-ranking.test.ts` (3 cases). The core case uses a fixture (`deep/nested/dir/config.ts` vs. `config-utils/index.ts`) deliberately chosen so the *pre-fix* scorer gets the order wrong (a shallow folder-prefix match outscores a deep filename match) — verified by temporarily reverting the bonus and confirming that case fails, then re-applying and confirming all 3 pass.
  - `tests/unit/skill-overrides-settings.test.ts` (7 cases): `isSkillOverrideValue`'s 4-literal acceptance, `updateSkillOverride`'s project-only scoping, "on" clearing the key instead of storing a redundant value, dropping the last override removing the `skillOverrides` key entirely, merging multiple overrides, and round-tripping other settings keys untouched.
  - No separate spec for the probe-list pin — it's a data-row addition with no new logic branch to unit-test, consistent with every other untested pinned-model entry in that file.
- playwright: 2 new specs, 0 updated.
  - `tests/e2e/cc-parity-2.1.280-at-mention-filename-ranking.spec.ts` — ranking-order assertion + in-context screenshot, against a discriminating fixture built in a disposable temp-dir workspace (the real repo checkout doesn't happen to contain a shape that discriminates pre/post-fix, mirrors `cc-parity-2.1.222-diff-raw-content.spec.ts`'s throwaway-workspace pattern).
  - `tests/e2e/cc-parity-2.1.280-skills-toggle.spec.ts` — source/token-estimate assertions, toggle-off then toggle-on round trip verified against the real `.claude/settings.json` file on disk (not mocked — only the SDK-dependent session/stream/context layer is fixtured via SSE mocking, since the settings write itself has no SDK dependency worth mocking away), plus an in-context screenshot.
- Not covered: the Opus 5.5 probe pin's live availability (the probe route spawns a real ephemeral SDK query — no unit/e2e test exercises a real model call anywhere in the suite, consistent with every other untested pinned-model probe entry).
- An adversarial review pass (code-reviewer subagent) caught that the first drafts of both the fs-list unit test and its e2e counterpart used fixtures that passed even without the fix (the "wrong" pre-fix order never actually occurred with those inputs) — both were rewritten with the discriminating fixture described above before finalizing.

## Risks / follow-ups

- **A second adversarial review pass on the skills toggle found two real races**, fixed before finalizing: (1) `SkillsOverlay`'s mount-time GET of current overrides could resolve after a user's toggle click and clobber the toggle response's fresher state — fixed with a `hasToggledRef` guard that makes the toggle handler's own response the sole source of truth for `overrides` once any toggle has fired; (2) two different skills toggled back-to-back would issue two concurrent POSTs racing the same read-modify-write `updateSkillOverride` call (no file lock — same shape as `updatePermissions`/`updateAutoMode`, so this isn't a new pattern, just newly exposed by a UI that lets you fire two of these quickly) — fixed by disabling every toggle button while any one toggle is in flight, closing the realistic single-client case. A true cross-tab/cross-client race on the same settings file remains open, same as every other settings-updater in this codebase; not addressed here since fixing it project-wide is out of scope for this release.
- **`/api/settings/skill-overrides`'s `req.json()` isn't wrapped in try/catch** (malformed JSON → 500, not 400) and `name` has no length cap. Left as-is to match `/api/settings/permissions`'s identical, pre-existing pattern rather than introduce one route that's hardened differently from its sibling — flagging in case a future pass wants to harden the whole family of settings routes together.
- **Skills toggle only exercises "on"/"off", not the full 4-state `skillOverrides` value.** The SDK's `Settings.skillOverrides[name]` accepts `"on" | "name-only" | "user-invocable-only" | "off"`; Claudius's UI only offers a binary on/off click, matching the simplest reading of upstream's "on/off state, with a click to change the state." `isSkillOverrideValue`/`updateSkillOverride` accept all four so a hand-edited `"name-only"` value round-trips instead of being clobbered, but there's no UI affordance to *set* the middle two states. Maximal alternative: a 4-way cycling toggle or a small dropdown per row — rejected as more UI complexity than the changelog entry's plain "on/off" wording asks for; revisit if a user asks for the middle states specifically.
- **Skills toggle is project-scoped only**, not per-user or per-local. Considered `"user"` scope (a personal preference) and `"local"` scope (untracked, machine-specific) — rejected in favor of `"project"` because this mirrors `enabledPlugins`' reasoning: "which skills are on for this workspace" reads as a team-visible decision that belongs in the checked-in `.claude/settings.json`, not a personal or untracked override.
- **A prior/parallel run of this exact pipeline left a `cc-parity/2.1.280-detached-from-sdk-0.3.280` branch** with a plan-only commit (run-notes, no code) that described the same three bucket-B items in "already shipped" language despite touching zero source files. Treated as a lead list, not evidence of finished work — every claim in it (the probe-list gap, the `/skills` overlay gap, the exact `SDK.d.ts` line for `skillOverrides`) was independently re-verified against this branch's own checkout before building anything. One claim in it didn't check out: it cited `/api/sessions/[id]/context`'s `skillFrontmatter` as already used elsewhere, when in fact only `ContextOverlay.tsx` (the `/skill-doctor` overlay) consumes it — still correct that the data exists and is reusable, just attributed to the wrong specific route file initially.
- **`node_modules` had `@anthropic-ai/claude-agent-sdk@0.3.280` installed at the time of this run**, but this repo's own `bun.lock`/`package.json` pin `^0.3.278` — the 0.3.280 copy on disk was almost certainly written by a concurrent SDK-update pipeline process sharing this working tree (see project memory on the updater's shared-tree behavior), not something this branch's own install produced. Before building the skills toggle, `Settings.skillOverrides`'s presence was re-verified against the *exact* 0.3.278 `sdk.d.ts` fetched from the npm registry (not the contaminated local copy) to avoid building against a feature that might not exist in the version Claudius actually depends on.
- **VSCode paste-marking / invisible-Unicode stripping (skipped).** Conservative shape shipped: nothing. Maximal shape: teach `PromptInput.tsx`'s `onPaste` to track paste spans for plain-text paste (not just file/image), strip invisible Unicode formatting/tag characters on ingest while preserving U+200C (ZWNJ, per the upstream Persian/Arabic-suffix fix elsewhere in this changelog), and surface both as real fields. Worth a dedicated follow-up rather than a partial build this cycle.
- **Language-less code block styling (skipped as ambiguous).** No docs page or changelog detail pins down the exact intended look beyond "colored like inline code"; Claudius's Shiki-highlighted no-language blocks already read as visually distinct from prose via border/copy-button chrome. Revisit if a concrete visual regression / user report shows up.
