# Claude Code parity 2.1.257 → 2.1.258

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

2.1.258 is a two-line bug-fix release: a macOS 12 launch regression fix
and a fix for scheduled/remote sessions failing with "user messages
must have non-empty content" after a re-sent permission approval could
not be applied. Neither entry introduces a new feature, setting, slash
command, or UI affordance, so there are **no bucket-B items this
release**. This is also a combined-mode run stacked on the
`sdk-update/0.3.257 → 0.3.258` bump on this same branch; I read
`.claudius/sdk-updater/run-notes/0.3.258.md` first and confirmed the
SDK side is a pure parity version bump with a byte-identical `.d.ts`
surface and no behavior change, so there was nothing already shipped
via the SDK migration to defer to for either entry. Headline risk to
flag for review: the second entry (permission-approval retry producing
empty message content on scheduled/remote sessions) describes a bug
class in a code path — permission-approval retry for scheduled/remote
sessions — that Claudius also reimplements itself (its own scheduler
and permission-approval flow in `lib/server/`), not something it gets
for free from the SDK's public API. I did not find evidence either way
that Claudius has (or lacks) the equivalent bug, and fixing a
speculative, undemonstrated bug is out of scope for this feature-parity
pipeline, so I've flagged it for a human to check under "## Risks /
follow-ups" rather than implement a fix here.

## Changelog classification

Both entries in the 2.1.258 changelog are pure bug fixes with no new
feature, setting, or UI surface — normally elidable per the "pure
bug-fix entries can be elided" rule, but both get a one-liner here
since one is trivially out of scope and the other needed real
investigation:

- `[C — CLI/terminal only]` Fixed Claude Code failing to launch on
  macOS 12 (Monterey), a regression introduced in 2.1.255 — this is a
  fix to the native CLI binary's own launch/startup path (an OS
  compatibility regression in the terminal executable). Claudius is a
  browser app with no equivalent launch mechanism; nothing to port.
- `[A — engine-side bug fix]` Fixed remote and scheduled sessions
  failing with "user messages must have non-empty content" after a
  re-sent permission approval could not be applied — this is a bug fix
  in the agent loop's message-assembly / permission-approval-retry
  logic, not a new feature or product surface, so it doesn't fit the
  bucket-B "ship it" action even though the *area* (permission
  approval, scheduled/remote sessions) is one Claudius product-side
  code reimplements. Checked `.claudius/sdk-updater/run-notes/
  0.3.258.md` first per the combined-mode instructions: it confirms
  0.3.258 is a pure parity version bump with a byte-identical `.d.ts`
  surface and explicitly states the upstream 2.1.258 changelog has "no
  actionable content" for the SDK pipeline, so this fix was **not**
  already shipped via the SDK migration on this branch — there's
  nothing to mark `[skip — already shipped via SDK migration]` against.
  Since there's no accompanying feature or reimplementable surface (the
  changelog entry is describing a bug in engine plumbing, not a
  capability a Claudius user gains or loses), there is no bucket-B
  action here. See "## Risks / follow-ups" for the recommended human
  follow-up on Claudius's own analogous code path.

## Implemented (bucket B)

- No bucket-B items in this release. Both upstream changelog entries
  for 2.1.258 are pure bug fixes (a macOS 12 CLI launch regression, and
  a message-content bug in the permission-approval retry path for
  scheduled/remote sessions) — neither adds a setting, slash command,
  UI affordance, or changed default for Claudius to reimplement. The
  first is CLI/terminal-only and doesn't translate to a browser app at
  all; the second is engine-side plumbing with no user-facing surface
  to expose. There is nothing to build this release.

## New UI surfaces

- No new UI surfaces this release. No bucket-B items were implemented
  (see above), and neither upstream changelog entry has a user-facing
  affordance for Claudius to expose — one is a native-binary launch
  fix, the other is invisible engine plumbing (message assembly on
  permission-approval retry).

## Tests

- vitest: 0 new specs, 0 updated. No bucket-B work shipped this
  release — nothing to cover. Ran the full suite against this branch
  to confirm nothing regressed: 174 files / 1805 tests, all passing.
- playwright: 0 new specs, 0 updated. No new or changed UI surface
  this release (see "## New UI surfaces"), so no screenshot spec is
  required. Did not run the full Playwright suite: this is a docs-only
  commit with zero source changes on top of an already-green branch
  (the sibling `sdk-update/0.3.258` run on this same branch was
  launched with `--skip-gates=e2e` and its own notes record `bun run
  test:e2e` already passing against this exact tree), and re-running
  the full chromium suite for a no-source-diff docs commit isn't a
  useful signal — `bun run lint`, `bun run test`, and `bun run build`
  were run instead and are all green.
- No new tests required beyond the above regression check. There is no
  new code path to exercise.

## Risks / follow-ups

- **Recommend a human spot-check Claudius's own permission-approval
  retry path for scheduled/remote sessions.** The upstream fix ("user
  messages must have non-empty content" after a re-sent permission
  approval could not be applied") lives in Claude Code's engine, not
  Claudius's code, and I did not find evidence Claudius has (or lacks)
  an analogous bug — I did not go looking inside `lib/server/session.ts`
  or the scheduler for a matching code path, because doing so would be
  inventing scope: this pipeline reviews Claude Code *feature* parity,
  not general bug-parity audits of Claudius's own reimplementations.
  Conservative choice made here: treat the entry as a pure upstream bug
  fix with no Claudius action (`[A — engine-side bug fix]`, no
  Risks-only speculative fix). Maximal alternative rejected: proactively
  auditing/patching Claudius's scheduled-session + permission-approval
  message-assembly code for the same failure mode on the chance it has
  an equivalent latent bug — rejected because the changelog gives no
  evidence Claudius's independent reimplementation shares the bug, and
  "fix a bug we haven't demonstrated exists" is out of scope for a
  feature-parity classification run.
- No bucket-B items shipped this release, so there are no delivery-shape
  alternatives to record.
- Working-tree note (not from this run): `git status` shows ~36
  pre-existing modified PNGs under `docs/cc-parity/*/*.png` (byte-size-
  only diffs, e.g. a prior screenshot regen / font-rendering pass) that
  predate this session. They were not touched by this run and are not
  committed here — flagging so a reviewer doesn't attribute them to
  2.1.258.
- This is a combined-mode run: this branch also carries the
  `sdk-update/0.3.257 → 0.3.258` bump (commit `26a3fab`) and the prior
  `cc-parity/2.1.257` work (commits `81c1c91`, `3e8b7d5`). Per
  `cc-parity-combine-into-sdk-pr` convention, this 2.1.258 docs commit
  belongs on that same combined branch/PR rather than a separate one —
  consistent with how this session found the branch already checked
  out.
