# Claude Code parity 2.1.209 → 2.1.210

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

2.1.210 is a small release: most of its substance is engine/CLI-internal
(Agent-tool hardening, auto mode's permission classifier, the bundled
dataviz skill, and — per this run's combined-mode note — the
Bash/PowerShell auto-background timeout message, which the sibling SDK
0.3.208→0.3.210 bump already shipped on this branch) or terminal-only
(screen-reader footer chrome, `←` list-navigation state in the CLI's
`agents` view). Four items are genuine product-surface gaps Claudius
reimplements this release: (1) the chat transcript's collapsed tool-call
row has no live elapsed-time indicator, unlike the Activity rail's
Task/Bash rows which already tick at 1Hz; (2) the Permissions page lets
users type `Write(path)` / `NotebookEdit(path)` / `Glob(path)` rules that
Claude Code itself now flags as unsupported path-scoped forms, with zero
in-app feedback; (3) Claudius's own `MEMORY.md` index writer
(`lib/server/auto-memory.ts` — confirmed independent of the SDK, which
ships no memory-tool code) appends forever with no size guard, so a
degenerate write today fails silently rather than erroring; (4) the
existing Shift+Tab permission-mode cycle has no assistive-tech
announcement when the mode changes. All four are minimal, additive
changes to screens Claudius already has. The headline risk to flag for
review is #4's shape: Claude Code gates this behind an opt-in
"screen reader mode" setting, and this run chose to always announce via
an `aria-live` region instead (see Risks) — a human should confirm that
default-on read is the right call for Claudius rather than porting the
opt-in toggle literally.

## Changelog classification

- `[B — reimplement in Claudius]` Added a live elapsed-time counter to
  the collapsed tool summary line — Claudius's `ToolCall.tsx` (the
  per-tool-call row in the chat transcript) shows only a static pulsing
  dot while running; `TaskBlock.tsx` and `BackgroundBashes.tsx` already
  established the 1Hz-ticking-elapsed-timer pattern for Task/Bash rows,
  so this closes the one collapsed-row type that lacked it.
- `[B — reimplement in Claudius]` Added a startup warning for
  `Write(path)`, `NotebookEdit(path)`, and `Glob(path)` permission rules
  — Claudius's `/permissions` page (`app/[workspaceId]/permissions/page.tsx`)
  is exactly where a user types free-text rules like these; added inline
  validation there instead of a "startup" check (Claudius has no CLI
  startup phase to hook).
- `[B — reimplement in Claudius]` Memory writes that leave a `MEMORY.md`
  index over its read limit now produce an explicit error instead of
  silent truncation — `lib/server/auto-memory.ts` is Claudius's own
  from-scratch reimplementation of Claude Code's project-memory index
  (confirmed the `@anthropic-ai/claude-agent-sdk` package ships no
  memory-tool or `MEMORY.md` logic at all — this is CLI-bundled, not
  SDK-exposed), and its `appendMemoryIndex` had no size guard whatsoever.
- `[B — reimplement in Claudify, conservative interpretation]` Screen
  reader mode now announces permission mode changes aloud when cycling
  with Shift+Tab — Claudius already has the identical Shift+Tab
  permission-mode cycle (`lib/shared/permission-modes.ts`,
  `components/chat/ModeSelector.tsx`); translated to the browser-native
  idiom (an always-on `aria-live` region) rather than porting a literal
  CLI "screen reader mode" toggle setting. See Risks for the rejected
  toggle-gated alternative.
- `[A — already via SDK updater]` Hardened the Agent tool against
  indirect prompt injection via content a subagent read — this is
  internal Agent-tool sandboxing inside the SDK's own tool
  implementation; Claudius invokes the Agent tool through the SDK and
  has no separate implementation to patch.
- `[A — already via SDK updater]` Improved auto mode: the permission
  classifier now defaults to Sonnet 5 for external sessions, validated
  on the session's first request and pinned for the session — the
  classifier itself is engine-internal logic reached via the SDK's
  `PermissionMode` (`"auto"` is a value Claudius's `permission-modes.ts`
  merely enumerates from the SDK's own type, not a classifier Claudius
  runs itself).
- `[A — already via SDK updater]` Improved the bundled dataviz skill's
  chart color validation with perceptual OKLab color difference and
  recalibrated color-blindness thresholds — bundled skills ship inside
  the CLI/SDK's own binary payload (confirmed: no skill files or
  `dataviz` references exist anywhere in the installed
  `@anthropic-ai/claude-agent-sdk` package tree — assets are extracted
  from the bundled binary at runtime via `extractFromBunfs.js`); Claudius
  does not hand-maintain a copy of this skill.
- `[skip — already shipped via SDK migration on this branch]` Improved
  the Bash/PowerShell tool message when a command hits its timeout and
  is auto-backgrounded, so the model can distinguish a hang from an
  explicit background request — this is `BashOutput.timedOutAfterMs`,
  which the sibling `0.3.208 → 0.3.210` SDK bump on this same branch
  already wired end-to-end (`lib/client/use-session.ts`'s
  `applyBashAutoBackground`, the "timed out" badge in
  `BackgroundBashes.tsx`; see `.claudius/sdk-updater/run-notes/0.3.210.md`).
  Re-implementing it here would conflict with that already-landed work.
- `[C — CLI/terminal only]` Agent view: the session you pressed ← from
  stays visibly marked even after mouse hover or arrow keys move the
  selection — this is `claude agents` TUI list-navigation state (arrow
  keys vs. mouse hover, tied to the terminal's `←` back-gesture).
  Claudius's Sessions/Agents surfaces are browser-routed pages with no
  equivalent modal list-selection state to preserve.
- `[skip — ambiguous, see Risks]` The agents footer hint now shows how
  many background agents are waiting on your input, with a brief color
  emphasis when the count changes — Claudius has no existing
  cross-session "waiting on permission input" aggregate signal to hang
  a badge off of (checked `lib/server/notifications-db.ts`,
  session/task state); building one from scratch is disproportionate to
  a footer hint and risks inventing scope the changelog entry doesn't
  actually specify the shape of. See Risks for the rejected concrete
  alternative.
- `[skip — operational note, not a feature]` Fable temporarily shows as
  unavailable in the advisor picker while a server-side issue causing
  Fable advisor failures is fixed — this documents a transient upstream
  status, not a shippable change.
- All other entries in the 2.1.210 block are pure bug fixes to
  engine/CLI-internal behavior (attach/session-transition races, MCP
  re-sync teardown, plugin cache writes, worktree lock sweeping, hook
  timeout mislabeling, background-worker crash loops, etc.) — elided per
  the "pure bug-fix entries can be elided" rule; none have a Claudius
  product surface to patch independently of the SDK.

## Implemented (bucket B)

- `[extend: chat transcript]` Live elapsed-time counter on the collapsed
  tool-call row. Files: `components/chat/ToolCall.tsx`,
  `lib/client/use-elapsed.ts` (new shared 1Hz-tick hook, factored out so
  `TaskBlock`/`BackgroundBashes`-style timers aren't reimplemented a
  third time), `lib/client/types.ts` (tool-call `startedAt` stamping),
  `lib/client/use-session.ts` (stamp `startedAt` when a tool_use first
  streams in). Rejected alternative: a global "Activity rail" timer
  applied retroactively to all rows — overkill; the gap was specifically
  the *inline transcript* row, which already has its own status dot.
- `[extend: /permissions page]` Inline warning when a typed rule starts
  with `Write(`, `NotebookEdit(`, or `Glob(`, suggesting `Edit(path)` /
  `Read(path)`. Files: `app/[workspaceId]/permissions/page.tsx`,
  `lib/shared/permission-rule-lint.ts` (new, pure — unit-testable
  without React). Rejected alternative: a blocking validator that
  refuses to add the rule — Claude Code's own behavior is a *warning*,
  not a rejection (the rule still works, it's just discouraged), so
  blocking would be stricter than upstream.
- `[extend: auto-memory writer]` `appendMemoryIndex` now refuses (with an
  explicit `413`-style error surfaced through the existing
  `/api/memory/auto` POST error path) to append past a size limit
  instead of growing `MEMORY.md` unbounded. Files:
  `lib/server/auto-memory.ts`, `lib/client/useAutoMemory.ts` (already
  propagated errors — no change needed there beyond confirming the path).
  Rejected alternative: silently truncating older entries — upstream
  explicitly replaced silent truncation with an explicit error, so
  mirroring silent truncation would regress relative to what Claude
  Code just fixed.
- `[extend: chat header / ModeSelector]` `aria-live="polite"` region
  announcing the new permission mode when it changes (Shift+Tab cycle or
  the dropdown). Files: `components/chat/ModeSelector.tsx`. Rejected
  alternative: gating this behind a new "Screen reader mode" setting —
  see Risks.

## New UI surfaces

- Elapsed-time badge on a running tool-call row, in context of the chat
  transcript (tab strip + message list visible). Screenshot:
  `docs/cc-parity/2.1.210/tool-call-elapsed.png`. Captured by
  `tests/e2e/cc-parity-2.1.210-tool-call-elapsed.spec.ts`.
- Inline "unsupported path-scoped rule" warning on the Permissions page,
  in context (scope tabs, syntax-hint box, rule columns visible).
  Screenshot: `docs/cc-parity/2.1.210/permission-rule-warning.png`.
  Captured by `tests/e2e/cc-parity-2.1.210-permission-rule-warning.spec.ts`.

## Tests

_(TODO — filled in after implementation.)_

## Risks / follow-ups

- **Screen-reader announcement shape (item 4).** Chose an always-on
  `aria-live` region over porting Claude Code's opt-in "screen reader
  mode" toggle. Reasoning: ARIA live regions are inert for sighted users
  and browsers/AT already let users control verbosity, so gating this
  behind a Claudius-specific setting only adds a discovery step for the
  assistive-tech users who need it. A human should confirm this
  default-on choice rather than the more literal toggle-gated port.
- **Agents footer "waiting on input" count (skipped, ambiguous).**
  Rejected concrete alternative: a badge on the "Agents" SideNav item
  mirroring the existing unread-notifications badge
  (`sidenav-workspaces-toggle-badge` pattern in `components/nav/SideNav.tsx`),
  fed by a new aggregate query over live sessions' permission-pending
  state. Not built this release because no such aggregate exists yet and
  the changelog entry doesn't specify enough about the intended shape to
  justify inventing the backend signal from scratch — flagging as a
  follow-up candidate, not implementing speculatively.
- **Working branch.** This run's harness prompt describes a
  "combined-mode" setup where the SDK migration and this cc-parity pass
  share one branch, but the branch actually checked out in this working
  tree is `sdk-update/0.3.210` (the SDK updater's branch), not a
  `cc-parity/2.1.210` branch — no such branch exists locally or on
  `origin` (`git ls-remote origin cc-parity/2.1.210` is empty). Per the
  prompt's own combined-mode framing ("SDK migration ... has just been
  completed on this same branch"), this run committed its work directly
  onto `sdk-update/0.3.210` rather than creating a separate
  `cc-parity/2.1.210` branch. A human should rename/re-target the
  branch before merge if the orchestrator expects the latter name.
- **`scripts/sdk-update/orchestrate.ts` was live in this same working
  tree** for part of this run (PID observed via `ps`, git history shows
  its two commits already landed). No local git operations were run
  against branch/HEAD while it was active with pending state; commits
  from this run were made once it was confirmed to not be actively
  mutating tracked source files. Flagging per house convention, not
  because anything broke.
