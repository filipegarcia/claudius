# Claude Code parity 2.1.207 → 2.1.209

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

The nominal range for this run is 2.1.207 → 2.1.209, but nearly all of that
range was already covered on this branch: a prior cc-parity pass shipped the
2.1.208 changelog's one bucket-B item (an "as of &lt;time&gt;" staleness note
on the Session cost & usage overlay, commits `6c4d3e7`/`f9a094f`/`a2d7a2a`,
documented in `.claudius/cc-parity/run-notes/2.1.208.md`), and everything
else in 2.1.208 was already classified bucket A/C there. The actual new
delta this run needs to evaluate is the single 2.1.209 changelog line: a
fix reverting an overly broad guard that was blocking `/model` and other
dialogs in `claude agents` background sessions. That's a pure bug fix in a
CLI-only surface (the terminal `claude agents` background-session manager,
which Claudius has no analog of) with no feature or UI shape to reimplement.
**Net result: zero bucket-B items, zero code changes this run.** Headline
risk to flag for review: the orchestrator's setup step claimed to have
checked out a fresh `cc-parity/2.1.209` branch from `origin/main`, but
neither was true at run start (see "## Risks / follow-ups" for the actual
branch-state discrepancy and how it was resolved).

## Changelog classification

**2.1.209 (the actual new delta):**

- `[C — CLI/terminal only] "Fixed /model and other dialogs being blocked in
  claude agents background sessions (reverts an overly broad guard)"` —
  this is a bug-fix revert inside the CLI's own `claude agents` background-
  session TUI (dialog-gating logic for a terminal-hosted background
  process manager). Claudius has no `claude agents`-style background
  session concept — its own long-running sessions are the browser app's
  primary session model, not a secondary "background" mode layered on top
  of a terminal session the way the CLI's is. No Claudius call site exists
  for this guard either way, so there's nothing to build or skip-via-SDK;
  it's simply not reachable from Claudius's architecture. (Could plausibly
  be read as `[A — already via SDK updater]` instead, since it's engine-
  side dialog plumbing — see Risks for the tie-break note.)

**2.1.208 (already fully classified and, where applicable, shipped in a
prior cc-parity run on this same branch — re-listed here only to satisfy
"cover every substantive entry," not re-implemented):**

- `[skip — already shipped via prior cc-parity 2.1.208 run on this branch]
  "/usage now shows your last-known usage bars with an 'as of' note when
  the usage endpoint is rate-limited, instead of an error screen."` —
  shipped as the staleness note + dimmed bars in `CostOverlay.tsx`; see
  `.claudius/cc-parity/run-notes/2.1.208.md` "## Implemented (bucket B)"
  for the full file list and design rationale.
- `[skip — already shipped via prior cc-parity 2.1.208 run on this branch]
  "Added screen reader mode: opt-in plain-text rendering for screen reader
  users."` — classified C there (no analog; Claudius's transcript is
  already semantic HTML), with a real narrower accessibility gap
  (`aria-live`/`role="log"`) flagged under that run's Risks instead of
  built as a ported "mode" toggle.
- `[skip — already shipped via prior cc-parity 2.1.208 run on this branch]
  "Added vimInsertModeRemaps setting."` — classified C there (no vim-mode
  concept in Claudius's composer).
- `[skip — already shipped via prior cc-parity 2.1.208 run on this branch]
  "Added mouse-click support for multi-select menus and 'Other' input rows
  in fullscreen mode."` — classified C there (Claudius's own modals already
  have native mouse support).
- `[skip — already shipped via prior cc-parity 2.1.208 run on this branch]
  "Added CLAUDE_CODE_PROCESS_WRAPPER ... corporate launcher wrapper."` —
  classified A there (transparent env-passthrough at Claudius's one CLI
  spawn site).
- `[skip — already shipped via prior cc-parity 2.1.208 run on this branch]
  Remaining ~38 "Fixed"/perf bullets (fast-mode restore, background-reply
  delivery, background-attach after binary replacement, GOAWAY crash,
  stream-json truncation, env-var scientific-notation parsing, markdown-
  table rendering cap, Edit/Read/Grep/Glob tool bugs, apiKeyHelper error
  surfacing, Bedrock streaming/SSO auth, stream-json hardening,
  /release-notes context-injection fix, memory leaks, permission-rule/MCP
  tool-pool caching, transcript/checkpoint pruning, background-agent daemon
  fixes, Ctrl+X worktree-deletion fixes, dangerous-command prompting,
  /install-github-app & /mcp background-session gating, MCP empty-URL
  classification, "Change directory" SDK-host fix)` — all classified A
  there (inherited transparently through the typed `Query`/`Options`/
  message surface, no Claudius reimplementation of the fixed logic).

Pure bug-fix entries with no changelog line at all in this run's diff (i.e.
strictly the 2.1.209 section beyond the one line above) do not exist —
2.1.209 has exactly one changelog entry.

## Implemented (bucket B)

- No bucket-B items in this release. The only feature-bearing release in
  this run's nominal range (2.1.208) was already fully implemented by a
  prior cc-parity run on this branch (see classification above and
  `.claudius/cc-parity/run-notes/2.1.208.md`). The actual 2.1.209 delta is
  a single CLI-only bug-fix revert with no product surface to build.

## New UI surfaces

- No new UI surfaces this release. No bucket-B work landed in this run
  (see above); the 2.1.208 UI surface (cost-overlay staleness note) was
  already shipped and screenshotted by the prior run
  (`docs/cc-parity/2.1.208/usage-staleness-note.png`,
  `tests/e2e/cc-parity-2.1.208-usage-staleness.spec.ts`).

## Tests

- No new tests required. No product code changed this run — see
  "## Implemented (bucket B)". The full gate suite (lint, vitest, build,
  Playwright chromium) was still run end-to-end against the branch to
  confirm this doc-only run doesn't regress anything; results recorded in
  the final message of this run.

## Risks / follow-ups

- **Branch-state discrepancy (process issue, not a product risk):** this
  prompt's setup section claimed the orchestrator had "checked out a fresh
  branch `cc-parity/2.1.209` from `origin/main`." Neither was true at run
  start — the working tree was on `sdk-update/0.3.208` (6 commits ahead of
  the actual `origin/main`, which is stale because that SDK-update PR
  hasn't merged yet), and `cc-parity/2.1.209` didn't exist. Branching from
  `origin/main` as instructed would have silently dropped the SDK 0.3.208
  bump and the already-shipped 2.1.208 cc-parity work — exactly the
  combined-mode context this run depends on to avoid re-implementing
  2.1.208's staleness-note feature. Resolved by creating
  `cc-parity/2.1.209` from `HEAD` (i.e. from `sdk-update/0.3.208`) instead.
  Flagging for a human to confirm this is the right base once the SDK PR
  merges (this branch will need a rebase/retarget onto `main` at that
  point, same as any other branch stacked on an unmerged PR).
- **A vs. C tie-break on the one 2.1.209 entry:** classified
  `[C — CLI/terminal only]` because the bug lives in the `claude agents`
  terminal background-session dialog-gating, a surface Claudius doesn't
  have. Could reasonably be tagged `[A — already via SDK updater]` instead
  (it's engine-side plumbing, transparently inherited either way) — same
  conclusion (no action) either tag, noted here per the "when unclear,
  document the assumption" rule rather than agonizing over the label.
- **Working tree had pre-existing, unrelated dirty state at run start:**
  ~18 binary PNG diffs under `docs/cc-parity/*` and `docs/sdk-updates/*`
  (re-encodings, not content changes tied to this run). These were
  `git restore`d before branching so they don't get swept into this parity
  PR. If they recur after the e2e gate run (some specs may regenerate
  screenshots), they should be restored again before considering the tree
  clean — not committed here, as they're out of scope for 2.1.209.
- No other risks identified — this is a doc-only run with no code changes.
