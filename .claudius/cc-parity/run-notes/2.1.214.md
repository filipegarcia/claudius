# Claude Code parity 2.1.211 → 2.1.214

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

_(TODO — filled in Step 6.)_

## Changelog classification

### 2.1.214

- `[A — already via SDK updater] Added the EndConversation tool` — a new tool surface baked into the agent core; Claudius renders arbitrary tool calls generically, so no client work is needed once the SDK exposes it.
- `[A — already via SDK updater] Added a periodic progress heartbeat for long-running tool calls` — an engine-side streaming behavior (keeps the turn alive during a slow tool call); Claudius's `ToolCall.tsx` already renders a live elapsed-time badge per tool call (`lib/client/use-elapsed.ts`), so the heartbeat just keeps that stream healthy — nothing new to build.
- `[B — reimplement in Claudius] Added an ISO "modified" timestamp to memory file frontmatter` — Claudius has its own reimplementation of Claude Code's per-project auto-memory files (`lib/server/auto-memory.ts`, `app/[workspaceId]/memory/page.tsx`, "Auto-memory" panel), independent of the SDK's own memory tool. We stamp/display `modified` in that frontmatter to match. See Implemented.
- `[C — CLI/terminal only] Added message.uuid, client_request_id, and tool_source attributes to OpenTelemetry log events` — Claudius has no OTel integration (no `@opentelemetry/*` dependency anywhere); this is an operator-facing telemetry knob for the CLI/server deployment, not a browser product surface.
- `[C — CLI/terminal only] Added CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH` — same reasoning; env-var tuning of a telemetry pipeline Claudius doesn't run.
- `[C — CLI/terminal only] Added reasoning effort to the subagentStatusLine payload` — the CLI statusline is a terminal-only rendering surface; Claudius has no statusline equivalent (subagent model/effort is already visible in its own subagent UI, e.g. session tasks).
- `[A — already via SDK updater] Added permission prompts for docker commands carrying daemon-redirect flags` — Bash permission-rule evaluation is engine-side; Claudius forwards Bash tool calls through the same SDK permission pipeline and inherits the new rule automatically.
- `[A — already via SDK updater] Changed single-segment dir/** hook if: conditions to match only <cwd>/dir` — hook/permission-rule matching semantics live in the SDK's rule engine.
- `[A — already via SDK updater] Changed file commands using -m/--magic-file or -f/--files-from to require permission` — Bash permission-rule change, engine-side.
- `[A — already via SDK updater] Changed SessionStart hooks to report source "fork" for forked sessions` — hook payload shape is defined by the SDK's hook dispatch, not something Claudius constructs itself.

### 2.1.212

- `[skip — ambiguous, see Risks] /fork now copies your conversation into a new background session; the in-session subagent it used to launch is now /subtask` — Claudius's existing `/fork` (`app/api/sessions/fork/route.ts` → `lib/server/sessions-store.ts`) already creates a new session copy, matching the *new* CC behavior. The paired `/subtask` (explicit in-session subagent spawn) is a genuinely new command with no Claudius equivalent; see Risks for why it's deferred rather than shipped this release.
- `[skip — ambiguous, see Risks] Added "claude auto-mode reset" to restore default auto-mode configuration` — CC's auto-mode has a richer per-tool allow-list configuration than Claudius's boolean `disableAutoMode` escape hatch (`lib/server/settings.ts`); a "reset" action needs a resettable config to reset. Ambiguous whether this should map onto Claudius's simpler toggle at all — see Risks.
- `[B — reimplement in Claudius] Added a session-wide limit on WebSearch tool calls (default 200, tunable via CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION)` — a runaway-loop safety net that is product-facing (protects the user's bill/session), not something baked into the SDK's own tool surface. Claudius reimplements it as a per-cwd setting extending the existing Limits screen instead of an env var. See Implemented.
- `[B — reimplement in Claudius] Added a per-session cap on subagent spawns (default 200, override with CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION)` — same reasoning, paired with the WebSearch cap; both extend `components/cost/LimitsPanel.tsx`. See Implemented.
- `[A — already via SDK updater] MCP tool calls running longer than 2 minutes now move to the background automatically` — this generalizes the existing SDK 0.3.210 Bash-timeout-backgrounding mechanism (already surfaced in Claudius via `BackgroundTasksPanel`/`background_tasks_changed`) to MCP tools; the underlying "move to background" runtime behavior is engine-side, and Claudius's existing background-task UI already renders whatever the SDK reports without change.
- `[skip — ambiguous, see Risks] Typing /resume in the agent view now opens a picker of past sessions — including sessions deleted from the list` — Claudius already has an equivalent picker (`components/chat/SessionPicker.tsx`) wired to `/resume`. The specific enhancement (surfacing *soft-deleted* sessions for restore) is a narrow edge case with no existing "deleted session" concept in Claudius's session store; conservative call is to skip rather than invent a restore-from-trash feature. See Risks.
- `[A — already via SDK updater] Deprecated the Task tool's mode parameter (now ignored)` — Task tool parameter handling is SDK-side.
- `[A — already via SDK updater] Changed Enterprise forceLoginMethod to be enforced for VS Code extension, SDK, setup-token, and install-github-app logins` — enterprise auth-policy enforcement lives in the SDK/auth layer Claudius delegates to.
- `[A — already via SDK updater] Changed session transcripts to record the reasoning effort level on each assistant message` — transcript/message schema change, engine-side; Claudius's transcript rendering already passes through whatever fields the SDK emits.
- `[A — already via SDK updater] Changed headless/SDK sessions to apply a set_model control request mid-turn` — this is explicitly an `@anthropic-ai/claude-agent-sdk` control-request feature; arrives with the SDK bump.
- `[C — CLI/terminal only] Changed agent view / claude agents --json: sessions waiting on a sandbox/MCP-input/managed-settings prompt now show "Needs input" instead of "Working"` — this is the CLI's terminal "agent view" grid status label; Claudius's own tab-status model (`running`/`idle`/`starting`/`error`/`background` in `SessionTabs.tsx`) is a different, already-existing affordance for the same underlying need (surfacing that a session needs attention), not a 1:1 port of this specific label.
- `[C — CLI/terminal only] Updated the auth status panel title from "Cloud authentication" to "Authentication"` — CLI's own `/status` panel copy; Claudius has its own differently-worded auth UI, not a shared string.

## Implemented (bucket B)

_(First-pass plan — revised in Step 6 with actual files/shape.)_

- `[extend: Memory screen] Auto-memory frontmatter "modified" timestamp` — plan: stamp `modified: <ISO>` on every auto-memory write/patch in `lib/server/auto-memory.ts`, surface it in `app/[workspaceId]/memory/page.tsx`'s file list and edit form.
- `[extend: Limits panel (Cost screen)] Session-wide WebSearch / subagent-spawn caps` — plan: extend `lib/server/limits-store.ts` + `app/api/limits/route.ts` + `components/cost/LimitsPanel.tsx` with `maxWebSearches`/`maxSubagents`, enforced via a new gate in `Session.canUseTool` (`lib/server/session.ts`).

## New UI surfaces

_(First-pass plan — revised in Step 6 with actual screenshot paths.)_

- Memory screen: "Last modified" label added to the auto-memory file list + edit form.
- Cost/Limits screen: two new numeric inputs in `LimitsPanel` for the WebSearch/subagent caps.

## Tests

_(TODO: vitest count, playwright count, anything explicitly not
covered with reason.)_

## Risks / follow-ups

_(TODO: what the next human should look at — design alternatives
you considered and rejected, items whose intent was ambiguous,
bucket-B items deferred to a follow-up. `- None identified.` is
a valid answer if you're sure.)_
