# Claude Code parity 2.1.275 → 2.1.276

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
-->

## Summary

Claude Code 2.1.276 ships exactly one changelog entry: a fix for a
2.1.275 regression where every request failed with `400 … Input tag
'advisor_20260301'` when `ANTHROPIC_BASE_URL` pointed at a proxy or
gateway. That's a pure bug fix inside the CLI/SDK's own request-building
path — not a new feature, setting, slash command, or UI affordance — so
it doesn't land in bucket A, B, or C at all; there is nothing here for
Claudius to reimplement, and nothing to mark "skip — via SDK updater"
either, because Claudius never constructs or forwards the affected
request data itself (`grep -rn "ANTHROPIC_BASE_URL|anthropic-beta|advisor_2026|input tag" lib/ app/`
returns zero hits — Claudius only ever calls `query()` from
`@anthropic-ai/claude-agent-sdk`, which owns that request path).
Consequently **no bucket-B work shipped this release** and no code
changed. The headline risk to flag for review isn't UX shape (there's no
UI here to get wrong) — it's sequencing: a sibling SDK-update pipeline
run for `0.3.276` (the SDK version that parities with CC 2.1.276) was
still in progress at analysis time, so whether it needs combining with
this branch per the existing "cc-parity → combine into SDK PR"
convention can't be settled yet. Since this branch has zero code changes,
that combining decision is likely moot in practice — see Risks.

## Changelog classification

The only changelog entry in this window is a pure bug fix, which the
run-notes template allows eliding — but it's the *sole* entry, so for
completeness:

- `[bug fix — elided per instructions] Fixed every request failing with
  400 … Input tag 'advisor_20260301' when ANTHROPIC_BASE_URL points at a
  proxy or gateway (2.1.275 regression)` — a regression fix in the
  CLI/SDK's own request-serialization path (a stray discriminated-union
  tag literal leaking into the request body when a custom base URL was
  configured). Not a feature/setting/command/UI surface, so A/B/C doesn't
  apply. Confirmed via `grep -rn "ANTHROPIC_BASE_URL|anthropic-beta|advisor_2026|input tag" lib/ app/`
  that Claudius never builds or forwards this request data itself — it
  delegates entirely to `@anthropic-ai/claude-agent-sdk`'s `query()`.
  Whenever the fix ships in an SDK release, Claudius inherits it with
  zero code changes; there is no Claudius-side workaround to remove
  either, since none was ever needed (Claudius doesn't set
  `ANTHROPIC_BASE_URL` to a proxy/gateway itself, and if a user's
  environment does, the bug — and its fix — live entirely upstream).

No other substantive entries exist in this changelog window.

## Implemented (bucket B)

- No bucket-B items in this release. The single changelog entry is a
  pure bug fix in the CLI/SDK's request-building path, not a new
  feature, setting, command, or UI affordance, and Claudius never
  constructs or forwards the affected request data itself (see
  classification above). There is no product surface here for Claudius
  to reimplement.

## New UI surfaces

- No new UI surfaces this release. The only upstream change is an
  internal bug fix with no user-visible feature or affordance attached
  to it, so there is nothing to screenshot.

## Tests

- No new tests required. There is no bucket-B behavior, setting,
  command, or UI element in this release to cover — the fix lives
  entirely inside the SDK's own request path, which is exercised by the
  SDK's own test suite, not Claudius's.
- Gates re-verified against this branch (in the isolated worktree — see
  Risks): `bun run lint` clean, `bun run build` succeeds, `bun run test`
  (vitest) 193 test files / 1993 tests all passing, all pre-existing —
  none added or modified, as expected for a docs-only change.
  `bun run test:e2e` was not run locally (this pipeline invocation was
  started with `--skip-gates=e2e`, and there is no new/changed UI
  surface for a new spec to capture per Step 5).

## Risks / follow-ups

- **Combined-PR convention — likely moot, but flagged for the human.** A
  sibling SDK-update pipeline run for `0.3.276` (the SDK version that
  parities with CC 2.1.276) was actively in progress at the time of this
  analysis (confirmed via `ps` and the presence of
  `.claudius/sdk-updater/run-notes/0.3.276.prompt.md` /
  `.transcript.jsonl` in the shared tree, with no finished
  `0.3.276.md` yet committed to any branch — a partial version of that
  file exists only inside a dangling autostash commit the pipeline made
  in the shared working tree, not on a real branch, so its final content
  is unknown and this run intentionally did not read or rely on it).
  Per the established "cc-parity → combine into SDK PR" convention, if
  that sibling PR does land real 2.1.276-relevant work, the two should
  ship as one combined release PR rather than two. In practice this is
  likely moot here: this branch has **zero code changes** (docs-only),
  so there is nothing to cherry-pick even if combined. Left
  for the human merging both to make the final call once the SDK-update
  run finishes and its branch/PR state is stable.
- **Gate/ROOT hazard — found and mitigated, please verify.**
  `scripts/cc-parity/orchestrate.ts` resolves its `ROOT` (used for the
  run-notes path it validates and the cwd it runs `lint`/`test`/`build`
  in) from `dirname(import.meta.url)/../..` at the location `bun run`
  loaded the script from — confirmed via `ps`/`lsof` that the live
  `orchestrate.ts` process (pid 49352 at analysis time) has cwd
  `/Users/filipegarcia/claudius`, the *shared* tree, not this branch's
  worktree. That shared tree's checked-out branch had been flipped to
  `sdk-update/0.3.275` by a concurrently-running SDK-update pipeline
  (confirmed via `ps`), yet still had a stray, untracked, placeholder
  copy of `.claudius/cc-parity/run-notes/2.1.276.md` sitting on disk (7
  `TODO` occurrences at analysis time) — leftover from before the branch
  got flipped. `.claudius/` is gitignored repo-wide (confirmed: `git add`
  on it warned "ignored by .gitignore"; `orchestrate.ts` even comments
  that this is deliberate so transcripts/run-notes "never get swept in"
  by other git ops), so that file is a plain on-disk artifact, not
  something a `git checkout`/stash in the shared tree can touch or pick
  up. All real work happened in a pre-existing `git worktree` at
  `/Users/filipegarcia/claudius-cc-parity-2.1.276`, correctly on
  `cc-parity/2.1.276` off `origin/main` (`e3cfefc` verified as an
  ancestor of `origin/main`) — `orchestrate.ts` itself has no `git
  worktree` logic, so this worktree must predate this invocation, likely
  from an earlier interrupted attempt at this same version. **Mitigation
  applied:** this run (a) committed and pushed `cc-parity/2.1.276` to
  `origin` so the real deliverable is durable regardless of local
  shared-tree state, and (b) mirrored this file's finished content
  byte-for-byte onto the shared tree's gitignored path at
  `/Users/filipegarcia/claudius/.claudius/cc-parity/run-notes/2.1.276.md`
  (write-only — no `git add`/commit/checkout there) so that if pid
  49352 (or whatever re-runs its gate) reads `ROOT`-relative from the
  shared tree, it sees this real content instead of the stale
  placeholder. Please verify the PR that lands references
  `cc-parity/2.1.276` on `origin` and reflects this file, not a stale
  local copy.
- No ambiguous entries this release — the single changelog line fully
  described both the regression and its fix, so nothing needed a `gh
  api` / docs lookup to disambiguate.
- None identified beyond the above.

