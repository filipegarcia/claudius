# Claude Code parity 2.1.270 → 2.1.271

<!--
  This file is the PRIMARY DELIVERABLE for the CC-parity bot.
  The orchestrator parses each `## ` section into the PR body.
  Replace EVERY `_(TODO …)_` placeholder with real content before
  finalizing — the gate fails the run if any section is still empty
  or placeholder.
-->

## Summary

_(TODO: one paragraph — which Claude Code features this release adds,
which ones Claudius reimplements in bucket B, the headline risk to
flag for review.)_

## Changelog classification

- `[skip — already shipped via SDK migration on this branch] Added omitClaudeMd to agent frontmatter and --agents JSON` — this same branch's `sdk-update/0.3.271` cycle shipped it end-to-end in commit `041c35f` (`lib/server/db-agents.ts` coercion + `.claude/agents/*.md` editor badge, e2e + unit test). Verified real code exists (not just run-note prose) via `git show 041c35f --stat`.
- `[A — already via SDK updater] Added per-command allowed_domains to Bash, PowerShell and Monitor in auto mode with sandboxing` — command execution and domain sandboxing happen entirely inside the CLI binary the SDK spawns; Claudius doesn't reimplement Bash/PowerShell/Monitor tool execution.
- `[A — already via SDK updater] Changed auto mode so a skill's/slash command's inline ! shell commands follow default-mode permission rules` — the auto-mode safety classifier is engine-internal.
- `[A — already via SDK updater] Changed auto mode so a subagent reports back via a dedicated hand-back call reviewed by the safety classifier` — same engine-internal classifier.
- `[A — already via SDK updater] Changed Monitor watches to always have a deadline, replacing persistent` — confirmed no call site for `MonitorInput`/`persistent` in the tree (also independently confirmed in this branch's `.claudius/sdk-updater/run-notes/0.3.271.md`, which audited `MonitorInput` directly); Claudius never renders/validates Monitor's tool-input shape.
- `[A — already via SDK updater] Improved dynamic workflows to pause on usage limit and resume automatically` — the dynamic-workflow orchestration loop lives inside the CLI/SDK's agent loop; Claudius only surfaces the `workflowSizeGuideline` setting passthrough (`lib/server/settings.ts:210`), not the orchestration engine itself.
- `[A — already via SDK updater] Changed the default dynamic workflow size to small on Pro plans, medium 15→10` — Claudius's `workflowSizeGuideline` setting is `undefined` by default (no Claudius-side hardcoded default that could contradict the new engine default), so the new default takes effect automatically once the SDK bumps.
- `[A — already via SDK updater] Improved claude mcp serve progress updates every 30s` — `claude mcp serve` turns the CLI itself into an MCP server for other clients; Claudius invokes `claude` as its own agent backend, never via this subcommand — not applicable, but if it were, it'd be engine-side CLI behavior.
- `[A — already via SDK updater] Improved Foundry/Claude Platform on AWS: alwaysLoad MCP server usable next turn without a tool-search round trip` — MCP tool-search/connection-state machinery is SDK-internal.
- `[A — already via SDK updater] Improved Markdown artifacts render as styled document pages` / `Improved Artifact tool publish errors` / `Improved Artifact tool capability-version error` / `Improved artifact watching to 10 (from 5)` — grepped the tree for an "Artifact tool" concept in `lib/server`/`lib/shared`/`components`: none exists. Claudius has no bespoke Artifacts feature; any Artifact-tool calls the model makes render through the existing generic SDK tool-call UI (`components/chat/ToolCall.tsx`), so richer artifact behavior from the engine arrives for free.
- `[A — already via SDK updater] Improved PDF @-mentions to say "page count unknown" instead of a guessed count` — PDF parsing happens inside the CLI's Read-tool implementation, not Claudius's own `@`-mention file picker.
- `[A — already via SDK updater] Changed Claude apps gateway/Bedrock/Vertex/Foundry sessions to no longer refresh a leftover claude.ai login` — engine-internal auth/session bootstrap.
- `[A — already via SDK updater] Updated the bundled claude-api skill` — bundled skill content ships inside the CLI/SDK's own catalog.
- `[B — reimplement in Claudius] Added support for a multiplier above 1, up to 10, in the modelPricing managed setting … for marked-up internal chargeback rates` — Claudius already reimplements `modelPricing` end-to-end (`lib/server/model-pricing-override.ts`, `app/settings/page.tsx`'s `ModelPricingCatalogField`) from CC 2.1.243, scoped to the *discount* framing (placeholder `"0.9"`, doc comments say "discount multiplier"). The math (`mult > 0 && Number.isFinite(mult)`) already tolerates values above 1, but the UI doesn't validate the new upstream cap (10) or communicate the markup use case at all. Extended (not net-new): relabelled copy, added a 10x ceiling with inline validation. See "Implemented".
- `[B — reimplement in Claudius, conservative scope] Improved the spinner status during long thinking: "deep in thought" after 45s` — Claudius has its own spinner-tip system (`lib/shared/tips.ts`, `components/chat/SpinnerTip.tsx`) and its own live elapsed-time ticker on the turn status (`components/chat/StatusLine.tsx`, `lib/client/use-elapsed.ts`) — this is Claudius's own UI, not an SDK-rendered terminal spinner, so the feature is product-side. Shipped the "deep in thought" half only (see Risks for the "picking the thought back up" and hook-feedback halves, which I judged already-covered / out-of-scope respectively).
- `[B — reimplement in Claudius, mostly already covered] …and shows "picking the thought back up" while recovering from the output-token limit` — Claudius's `api-retry.ts` already carries a `max_output_tokens` reason (SDK's `SDKAssistantMessageError`) and `SpinnerTip` already preempts the rotating tip with the retry reason ("an output-length limit") while that retry is in flight. Judged this close enough to the upstream intent (an accurate status replacing the generic tip while stalled on the same condition) that a copy-only change wasn't worth a separate shipped bullet; no code changed for this half.
- `[skip — ambiguous, see Risks] Improved hook feedback: spinner names the running SessionStart/UserPromptSubmit/PreToolUse/SessionEnd hook with elapsed time; Esc cancels a prompt waiting on a SessionStart hook` — Claudius has no server-side hook-execution timing/instrumentation today (grepped `lib/server/session.ts` for hook registrations — the existing `PreToolUse` hook wiring around line 2770 does not track start/elapsed or broadcast an event). Building this needs a new SSE event type across every hook registration site plus Esc-cancel wiring — a distinct, larger unit of work. See Risks for the conservative-vs-maximal call.
- `[skip — ambiguous, see Risks] Added a spinner tip pointing Bedrock/Vertex/Foundry/LLM gateway users to the Claude desktop app; the claude.ai desktop tip now suggests /desktop` — this is Anthropic's own cross-sell from the CLI/terminal to a *different* native app. Claudius doesn't have an analogous "you're in the browser, try our desktop build" nudge, and inventing one wasn't supported by this changelog entry's actual scope (it's about Anthropic's distribution funnel, not a generic tips pattern). See Risks.
- `[C — CLI/terminal only] Added fast mode in Claude Code Remote sessions (cloud and self-hosted runners)` — this is Claude Code Remote's own cloud/self-hosted-runner infrastructure, a different product surface than Claudius; Claudius already has local fast mode (`fastModeState` in `StatusLine.tsx`) from an earlier release, unaffected by this entry.
- `[C — CLI/terminal only] Added mouse support to the /config panel in fullscreen mode` — TUI-only interaction model.
- `[C — CLI/terminal only] Added claude self-hosted-runner --drain-marker-file` — self-hosted-runner CLI flag, telemetry only.
- `[C — CLI/terminal only] Added --accept-command <sha256> to claude plugin install/update` — CLI flag.
- `[C — CLI/terminal only] Changed the IDE selection indicator in the prompt to a [⧉ …] pill` — terminal prompt rendering.
- `[C — CLI/terminal only] Improved /mobile to show a single QR code` — native mobile app promo; no Claudius mobile client to link.
- `[C — CLI/terminal only] Improved Remote Control to leave fewer empty sessions on claude.ai when setup fails` / `Improved the Claude in Chrome message in cloud sessions when the browser can't be reached` — claude.ai Remote Control / Claude-in-Chrome cloud infra, not Claudius.
- `[C — CLI/terminal only] All [VSCode] / [Claude Code on the web] / [Claude Tag] / [Code Review] entries` — distinct Anthropic products/surfaces (VS Code extension, claude.ai/code, Slack/Teams bot, PR review bot); none of these are Claudius.
- Pure bug-fix entries (org-policy caching, Bash permission-check edge cases, MCP OAuth races, `/resume` file-tracking, terminal rendering perf, Windows PowerShell temp-path, etc.) are engine-internal and elided per the instructions — they all land automatically via the SDK updater with no Claudius action needed. One exception worth naming: `Fixed spinner tips suggesting commands that aren't available for your account type or are disabled in your session` — Claudius's own `selectTips()` (`lib/shared/tips.ts`) already gates every command-carrying tip against `availableCommands`, so this bug never had a Claudius-side equivalent to fix.

## Implemented (bucket B)

- `[extend: Settings → modelPricing catalog field]` Pricing multiplier up to 10x for markup/chargeback — `app/settings/page.tsx` (`ModelPricingCatalogField`), `lib/server/model-pricing-override.ts`, `lib/server/settings.ts`. Rejected alternative: a brand-new "Billing" settings screen — overkill, this is one field's validation/copy inside a catalog editor Claudius already ships.
- `[extend: chat session status line]` "Deep in thought" status label after 45s of continuous turn time — `components/chat/StatusLine.tsx`. Rejected alternative: a new dedicated "thinking indicator" component — overkill, StatusLine already owns the turn status label and the elapsed-time ticker this reuses.

## New UI surfaces

- `docs/cc-parity/2.1.271/model-pricing-multiplier.png` — captured by `tests/e2e/cc-parity-2.1.271-model-pricing-multiplier.spec.ts`. Settings page, modelPricing catalog field expanded, showing the relabelled "Pricing multiplier" input with markup copy and an out-of-range (11x) value rejected.
- `docs/cc-parity/2.1.271/deep-in-thought-status.png` — captured by `tests/e2e/cc-parity-2.1.271-deep-in-thought-status.spec.ts`. Chat session view, StatusLine mid-turn at 45s+ elapsed (via `page.clock`), showing "Deep in thought" in place of "Working", full header chrome around it.

## Tests

_(TODO: vitest count, playwright count, anything explicitly not
covered with reason.)_

## Risks / follow-ups

_(TODO: what the next human should look at — design alternatives
you considered and rejected, items whose intent was ambiguous,
bucket-B items deferred to a follow-up. `- None identified.` is
a valid answer if you're sure.)_
